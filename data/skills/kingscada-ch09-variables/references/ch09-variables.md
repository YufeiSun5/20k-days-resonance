第九章  变量定义和管理

# 第九章 变量定义和管理

---

第九章  变量定义和管理

## 9.1      概述

数据库是KingSCADA最核心的部分。在KingSCADA运行时，工业现场的生产状况要以动画的形式反映在屏幕上，同时应用人员在计算机前发布的指令也要迅速送达生产现场，所有这一切都是以实时数据库为中介环节，数据库是联系上位机和下位机的桥梁。

---

第九章  变量定义和管理

## 9.2      变量类型

KingSCADA系统中定义的变量与一般程序设计语言（如BASIC、PASCAL、C语言）中定义的变量有很大的不同，既能满足程序设计的一般需要，又考虑到工控软件的特殊需要。KingSCADA目前支持的变量类型如下：

系统变量：是系统已经内置好的变量，这些变量无须定义，用户可以直接使用，每个变量点是一个单独的变量对象，不能修改。

用户变量：是用户自己创建的变量，包括基本变量，引用变量

         
基本变量：基本的变量点，每个变量点是一个单独的变量对象。

         
引用变量：以一组变量，替代多组数据类型相同的变量。

---

第九章  变量定义和管理

### 9.2.1     系统变量

系统变量的类型包括如下几种：

**$Year****：**16位无符号整型，返回系统当前日期的年份。

**$Month****：**16位无符号整型，返回1到12之间的整型，表示当前日期的月份。

**$Day****：**16位无符号整型，返回1到31之间的整型，表示当前日期的日。

**$Hour****：**16位无符号整型，返回0到23之间的整型，表示当前时间的时。

**$Minnute****：**16位无符号整型，返回0到59之间的整型，表示当前时间的分。

**$Second****：**16位无符号整型，返回0到59之间的整型，表示当前时间的秒。

**$MillSecond****：**16位无符号整型，返回0到999之间的整型，表示当前时间的毫秒。

**$Date****：**返回系统当前日期字符串，最长32个字符。

**$Time****：**返回系统当前时间字符串，最长32个字符。

**$User****：**在程序运行时记录当前登录的用户的名字，最长32个字符。

**$DayofWeek****：**返回0-6之间的整数，表示当天是星期几，0表示星期天

**$StartHistoryRecord****：****布尔型，表示历史记录是否启动，****1****：启动，****0****：未启动，应用人员在开发应用时，可通过脚本预先设置该变量为****1****，在应用运行时刻也可以进行控制。**

**$StartAERecord****：**布尔型，表示报警记录是否启动，1：启动，0：未启动，**应用人员在开发应用时，可通过脚本预先设置该变量为****1****，在应用运行时刻也可以进行控制。**

**$NewAlarm****：**布尔型，每当报警发生时，系统将该变量自动设置为1，由应用人员负责把该值恢复到0。网络模式下，在客户端工程运行时不能对服务器工程的系统变量$NewAlarm进行置0的写操作。

**$RedundantStatus****：****16****位有符号整型，表示当前应用处于哪种冗余状态：**

**变量****=0****表示非冗余状态**

**变量****=1****表示冗余主机激活状态**

**变量****=2****表示冗余主机非激活状态**

**变量****=-2****表示冗余从机激活状态**

**变量****=-1****表示冗余从机非激活状态（监视状态）**

**变量****=**3表示主机激活，从机异常或网络异常

**变量****=**-3表示从机激活，主机异常或网络异常

**应用运行时可由应用人员手动修改数值来切换冗余状态。**

**$AlarmUpgradeTimeInterval****：**表示发生报警后未处理的时间，默认值为1800（秒）。

**$AlarmUpgradeAckTimeInterval****：**表示报警确认后未处理的时间，默认值为86400（秒）。

说明:关于报警升级的具体使用，请参见12.6.2.。

**$ServerStatus****：**16位有符号整型，表示客户端与服务器的网络连接状态，其状态值含义如下：

状态值=0，表示为初始值，未获得网络连接状态

状态值=1，表示为连接主机主网通道

状态值=2，表示为连接主机从网通道

状态值=3，表示为连接从机主网通道

状态值=4，表示为连接从机从网通道

状态值=-1，表示为其它原因

状态值=-2，表示无法连接到服务器，服务器所有通道均ping不通

状态值=-3，表示无法连接到服务器，客户端授权已满

状态值=-4，表示无法连接到服务器，未找到可用的授权

状态值=-5，表示无法连接到服务器，没有激活的服务器

状态值=-6，表示无法连接到服务器，服务器未运行

**注意：**$ServerStatus变量只能在客户端使用，如果在服务端使用，值始终为0。

---

第九章  变量定义和管理

### 9.2.2     基本变量

基本变量数据类型包括：

I/O变量：布尔型、实数型、整数型、字符串类型

内存变量：布尔型、实数型、整数型、字符串类型

这八种基本类型的变量是通过变量属性对话框定义的，同时在变量属性对话框的属性卡片中设置它们的部分属性。

基本变量数据类型表如下：

|  |  |  |  |
| --- | --- | --- | --- |
| 数据类型 | 描述 | 取值范围 | 精度 |
| 内存离散型 | bool型 | 0 (false)，1 (true) | 精确 |
| IO离散型 |
| 内存整型 | integer型 | -2147483648~2147483647 | 精确 |
| IO整型 |
| 内存长整型 | Long型 | -999999999999999~  999999999999999 | 精确 |
| IO长整型 |
| 内存实型 | float型  单精度浮点 | -3.402823466e+38  ~3.402823466e+38 | 精确度6位 |
| IO实型 |
| 内存双精度浮点型 | Double型，  双精度浮点类型 | -1.7976931348623158e+308~  1.7976931348623158e+308 | 精确度15位 |
| IO双精度浮点型 |
| 内存字符串型 | string型 | 最大长度128个字符 | 精确 |
| IO字符串型 |

---

第九章  变量定义和管理

#### 9.2.2.1        新建基本变量

在KingSCADA的工程设计器的树形目录下选择“建点”→“数据词典”选项，右侧的内容显示区会显示当前应用中所定义的变量和系统变量。单击“新建”按钮，弹出“变量属性”对话框，如图9- 1所示：

图9- 1 新建基本变量对话框

此对话框中默认有三个属性卡：基本属性卡、历史属性卡和报警属性卡。若在数据类型下拉框中选择了IO变量。将会增加一个IO属性卡。

---

第九章  变量定义和管理

#### 9.2.2.2        基本属性定义

基本属性卡中的各项用来设置变量的基本特征，如图9-1所示，各项含义如下：

**变量名：**设置变量的名称，变量名里可以包含英文字母、数字、中文字符等。

变量名称规范要求：

1、同一应用中的变量不能重名

2、变量名区分大小写

3、变量名最长不能超过64个字符

4、第一个字符不能是数字

5、不能包含空格字符和如下28个无效字符：,:;+-\*/%&!~|^<>={}[]().`’
”\?

6、不能包含如下26个片假名字符：

ゴ
ガ ギ グ ゲ ザ ジ ズ ヅ デ ド ポ ベ プ ビ パ ヴ ボ ペ ブ ピ バ ヂ ダ ゾ ゼ

例如：温度、压力、液位、var1等均可以作为变量名。

**变量类型：**分为基本类型和引用类型两种，可任选其中的一种，这里选择基本类型。

**数据类型：**设置变量的数据类型，在下拉框中选择其中的一种。

**描述：**设置变量的描述信息，限制128个字符。

**死区：**数据类型为整型或实型时此项有效，只有当该数据变量值的变化幅度超过死区中设置的数值时，KingSCADA才更新与之相连接的画面显示，缺省为0。

**初始值：**此项内容与所定义的变量类型有关，定义模拟量时出现编辑框可输入一个数值，定义离散量时出现开或关两种选择。定义字符串变量时出现编辑框可输入字符串，它们规定软件开始运行时变量的初始值。

**最小值/****最大值：**设置整型或实型变量的量程范围。默认为-999999999~~999999999

**工程单位：**设置整型或实型变量的工程单位，限制8个字符。

**保存值**： 该项被选中后，那么运行态下，保存值是当运行系统退出时会将退出时刻的值保存到变量定义的初始值中，作为下次运行时的初始值。

**保存参数**：该项被选中后，系统运行时，如果变量的域（可读可写型）值发生了变化，KingSCADA运行系统退出时，系统自动保存修改的域值。运行系统再次启动后，变量的初始域值为上次系统运行退出时保存的值，否则，系统不会保存退出前修改的域值。

***注意：变量的保存参数，起作用的区域是变量定义时，变量属性里出现的属性可以保存，如高限报警、低限报警等，变量属性里没出现的属性如AlarmEnable******，则不能保存。***

**允许其他应用访问：**该项被选中后，变量可以被第三方程序访问，如DDE、OPC、API，否则该变量不能够被第三方程序访问。

**记录事件：**该项被选中后，那么运行态下，用值输入动画、鼠标事件、图素、控件的事件脚本里导致该变量值域或其它域的值发生变化，都会生成操作事件，操作事件会在事件窗口中显示，也可以保存到报警事件库里，使用户能够了解到该变量的操作变化情况。详细介绍请参见第12章报警与事件系统。

**安全区：**设置变量所属的安全区，只有拥有这个安全区的用户才能操作该变量，详细介绍请参见第6章用户安全管理系统。

---

第九章  变量定义和管理

#### 9.2.2.3        历史属性定义

历史记录属性卡中的各项是用来设置变量的保存方式，具体介绍请参见第23章KingSCADA历史记录。

---

第九章  变量定义和管理

#### 9.2.2.4        报警属性定义

报警属性卡中的各项是用来设置变量的报警属性，具体介绍请参见第12章报警与事件系统。

---

第九章  变量定义和管理

#### 9.2.2.5        IO属性定义

新建IO变量时需要具备以下几个条件，换一种说法就是要经过下面的步骤才能在服务端应用中创建IO类型的变量。

步骤一：建IOServer应用。具体使用方法详见第8章。

步骤二：在IOServer应用中建设备。具体使用方法详见第8章。

步骤三：在IOServer应用中建变量。具体使用方法详见第8章。

步骤四：在IOServer应用中进行网络配置。具体使用方法详见第19章19.2.3节内容。

步骤五：在服务端应用进行网络配置，连接IOServer应用。

在其它服务器－IOServer服务器中，添加IOServer站点。具体使用方法详见第19章19.2.1节内容。

IO属性选项卡中的各项用来设置IO变量的基本特征，如图9-2所示：

图9- 2  IO属性选项卡

各项含义如下：

**访问名称**：由“IO站点名称.IO变量名称.变量域”组成的。用户可以直接输入，也可以单击后面的按钮选择关联的IOServer站点、IO变量名和变量的域名，不可以为空，站点名称+IO变量名称+域名的长度不能超过128个字符（超过的话，输入的将不会显示）。这里只做长度限制和是否存在的校验，其它的校验工作在IOServer中完成。

单击“访问名称”后面的按钮，弹出 “IO变量选择器”对话框。如下图9-3所示：

图9-3  IO变量选择器

“IO变量选择器”对话框的左侧树型目录列举了所有已添加的IOServer服务器站点及其IOServer应用具有的三种类型的变量：系统变量、设备变量和用户变量。

1、当选择站点下的“系统变量”时，右面的变量列表会显示出该IOServer应用自带的系统变量“$Sys$LocalIOSvr”，如下图9-4所示。

图9-4  IO变量选择器

选中“$Sys$LocalIOSvr”变量，单击变量域后面的下拉式列表框的下拉箭头，则显示出如图9-5所示的系统变量具备的变量域，可以连接的变量域有：Year、Month、Day、Date、DayOfWeek、Hour、Minite、Second、Time，以上的含义与9.2.1中的系统变量含义相同，区别在于这些是IOServer应用的系统变量，RunControl和RunAtatus两个域保留但不能使用。

图9-5  IO变量选择器

2、当选择站点下的“设备变量”时，右面的变量列表会显示出该IOServer应用中定义的所有设备，如下图9-6所示。

图9-6  IO变量选择器

任选一个设备，单击变量域后面的下拉式列表框的下拉箭头，则显示出如图9-7所示的设备变量具备的变量域，可以连接的变量域有：CommControl、CommStatus、DeviceAddr。

CommControl：设备采集控制，Bit数据类型，只写，true----开始采集，false---暂停采集。

CommStatus：设备运行状态，Short数据类型，只读，0--正常，1--暂停采集（commControl暂停采集），2---故障（与设备通讯失败），3--设备未初始化之前的默认值，？？---停止采集(IOServer停止)。

DeviceAddr：设备地址，String数据类型，可读可写。

BadOccur，CommMasterDevice和CommSlaveDevice三个域保留但不能使用。

图9-7  IO变量选择器

3、当选择站点下的“用户变量”或用户变量下面的变量组时，右面的变量列表会显示该变量组的所有变量，如图9-3所示。

任选一个变量，单击变量域后面的下拉列表框的下拉箭头，在变量域下拉列表框中列出选中变量的所有变量域，默认选择Value变量域。具体变量域的含义请参考9.3.1节说明，

图9-8 
IO变量选择器

使用IO站点的变量描述：选择该复选框，则会将选择的IO变量的描述作为新建变量的描述。单击“确定”按钮将会把选择的IO变量的描述填充到“变量属性”对话框的“描述”文本框中，如果“描述”文本框中已有内容，则会进行覆盖，填充后的“描述”文本仍然允许用户编辑。该复选框默认为选中状态。

变量列表中列出变量名称、类型、描述、设备、寄存器。选择某个变量和该变量的变量域（默认为Value域）后，单击“确定”按钮，站点名称、IO变量名称和变量域会按照“访问名称”的组成规则自动填充到“访问名称”文本框中。

当更改站点或IO变量的名称时，访问名称并不会自动更新，再次编辑或者查看IO配置页面时，才会对该域做校验。当IOServer服务器站点不存在时，弹出如下图9-4所示的对话框。

图9-9

当IOServer服务器站点中的变量名不存在时，弹出如下图9-10所示的对话框。

图9-10

当IOServer服务器站点中的变量域不存在时，弹出如下图9-11所示的对话框。

图9-11

设置好变量的属性和访问名称后，单击“确定”按钮会校验“访问名称”中的变量域的类型和变量的数据类型是否一致，不一致的话弹出提示框：变量数据类型不一致！。

**采集设置：**分为允许、强制读、强制写三个复选项：

**允许：**控制变量采集与否，此项被选中表示KingSCADA运行时采集该变量的值，否则不采集变量的值。用户也可以在运行环境中通过IOEnable域动态更改该属性值。

**强制读：**此项为复选项，当该项被选中时，每次采集（数据上报），不管数据是否发生变化，都要强制记录、报警和进行其它处理。用户也可以在KingSCADA运行环境中通过IORead域动态更改该属性值。

**强制写：**此项为复选项，当此项被选中时，在KingSCADA画面中只要用户重新设置、输入了变量值，不管是否发生了变化，KingSCADA都完成一次写操作，将数据写到设备上。用户也可以在运行环境中通过IOWrite域动态更改该属性值。

**写回馈：**此项为复选项，当此项被选中时，对io变量进行写操作后，会获取写回馈结果成功或是失败信息。如果配置了记录操作事件，会同步产生一条操作记录事件。

定义完变量的属性后，单击“确定”按钮，新建的变量会增加到数据词典中。

---

第九章  变量定义和管理

#### 9.2.2.6        IO变量数据转换

**1****、IO****整型和IO****实型**

IO整型和IO实型的变量，在其IO标签页中，KingSCADA提供了线性、开方、查表（非线性表）、累计4种转换方式，如果只需要一次数据转换，则一般在SCADA的属性页选择相应的转换方式即可，不需要在IOServer中配置，但若需要做两次数据转换，则可以在IOServer与SCADA中都配置数据转换。其中线性、开方、查表（非线性表）的数据转换方式，请参考第8章的内容。这里我们简单介绍一下“累计”方式的数据转换方式。

在变量属性中的IO标签页，单击数据转换设置的“其他配置”按钮，弹出如下对话框。

图9-12 数据转换的累计功能定义对话框

累计是在工程中经常用到的一种工作方式，经常用在流量、电量等计算方面。KingSCADA的变量可以定义为自动进行数据的累计。KingSCADA提供两种累计算法：直接累计和差值累计。选择直接累计或者差值累计后，先对采集到的IO数值进行转换，这里包括线性转换和开方转换，而且线性转换使用的是工程最大最小值。累计计算时间与变量采集频率相同，对于两种累计方式均需定义累计后的值的最大最小值范围，当累计后的变量的数值超过定义的最大值时，变量的数值将恢复为该对话框中定义的最小值。上图显示了累计后的值的最小值和最大值的定义。

**直接累计：**

从设备采集的数值，经过转换后直接与该变量的原数值相加。计算公式为：

变量值=变量值+采集的数值转换后的值

例如：管道流量S计算，采集频率为1000ms，5秒钟之内采集的数据经过转换后值依次为S1=100、S2=200、S3=100、S4=50、S5=200，那么5秒钟内直接累计流量结果为：

S=S1+S2+S3+S4+S5,即为650。

**差值累计：**

变量在每次进行累计时，将变量实际采集到的数值做转换并与上次的数值求差值，对其差值进行累计计算。当本次转换后的数值小于上次数值时，即差值为负时，将通过变量定义的画面中的最大值和最小值进行转化。

差值累计计算公式为：

显示值=显示旧值+（采集新值转换后的数值-旧值） (公式一)

当变量新值小于变量旧值时，公式为：

显示值=显示旧值+（采集新值转换后的数值-采集旧值）+（变量最大值-变量最小值） （公式二）

注：当本次采集的工程值小于上次工程值时，采用公式2。

变量最大值是在变量属性定义画面最大最小值中定义的变量最大值。

例如：条件如上例，变量定义画面中定义的变量初始值为0，最大值为300。那么5秒钟之内的差值累计流量计算为：

第1次：S(1)=S(0)+
(100-0)=100

（采用公式一）

第2次：S(2)=S(1)+
(200-100)=200

（采用公式一）

第3次：S(3)=S(2)+
(100-200)+(300-0)=400

（采用公式二）

第4次：S(4)=S(3)+
(50-100)+(300-0)=650

（采用公式二）

第5次：S(5)=S(4)+
(200-50)=800

 (采用公式一) 

即5秒钟之内的差值累计流量为800。

注：累计计算是按照变量采集频率进行累计的，无论变量是否变化，都要按照采集频率进行累计。

**2****、IO****离散**

IO离散变量的数据转换设置提供了：无和反相。无，即是不转换，是指从设备中采集上的离散数据直接在工程中应用；反相，是指从设备中采集上的离散数据经过取反操作后在工程中应用，即采集上来的原始数据为0，转换后的工程值为1，采集上来的原始数据为1，转换后的工程值为0。

图9-13 离散变量IO标签页数据转换设置

---

第九章  变量定义和管理

### 9.2.3     引用变量

引用变量是KingSCADA新引入的一种内存变量类型，引用变量类似于指针，只是指向另外一个变量。引用变量也有类型，就是被引用变量的类型，可以是整型、实型（普通变量），也可以是离散变量等。通过引用变量，可以实现使用一组变量替代多组变量的功能，大大减少了工作量，提高了工作效率，也会使整个画面变得整洁、美观。引用变量不能查询历史数据。

---

第九章  变量定义和管理

#### 9.2.3.1        新建引用变量

在工程设计器的树形目录下选择“建点”→“数据词典”选项，在右侧内容显示区中单击“新建”按钮，弹出“变量属性”对话框，在对话框中的“变量类型”选项中选择“引用”类型，即可对引用变量进行定义，如图9-26所示：

图9-14 新建引用变量对话框

引用变量的定义比较简单，只须设置变量的名称、数据类型及描述信息即可，但要注意数据类型与被引用的变量类型一致。

---

第九章  变量定义和管理

#### 9.2.3.2        引用变量的使用

引用变量建立后，只有建立引用才可实现引用变量的功能。

建立引用：引用变量名=&被引用变量名，如RefVar = &Tag；

解除引用：引用变量名= null，如RefVar
= null；

举例：

第一步：在数据词典中建立一个引用变量：

变量名：RefVar

变量类型：引用变量

数据类型：内存整型

第二步：在数据词典中建立三个IO变量：

变量名：Tag1、Tag2、Tag3

变量类型：基本变量

数据类型：IO整型

其他属性根据需要定义。

第三步：在画面上添加一个文本图素和三个按钮：

文本图素设置“模拟值输出”动画链接：\\local\RefVar

三个按钮的鼠标左键弹起脚本程序分别为：

[**\\local\RefVar=&\\local\**](file:///\\local\RefVar=&\local\)Tag1；

[**\\local\RefVar=&\\local\**](file:///\\local\RefVar=&\local\)Tag2；

[**\\local\RefVar=&\\local\**](file:///\\local\RefVar=&\local\)Tag3；

第四步：进入运行环境，单击第一个按钮后文本图素中显示Tag1变量的值；单击第二个按钮后，文本图素中显示Tag2变量的值；单击第三个按钮后显示Tag3变量的值。

***注意******：结构和模型对象的远程引用要求成员的名称、类型、顺序一致。***

---

第九章  变量定义和管理

### 9.2.4    局部变量

局部变量是指在一些功能模块中定义的变量，变量的使用范围只是在该模块中，不能被模块外的地方引用。KingSCADA局部变量包括：

画面的局部变量：指在画面脚本程序中（包括：画面打开时、画面运行时、画面关闭时）定义的局部变量。

模型的局部变量：指在数据模型中定义的变量。

脚本的局部变量：指在脚本程序中定义的局部变量。

精灵图的局部变量：指在精灵图中定义的变量

局部变量的数据类型包括：

String：基本变量，字符串类型

Bool：基本变量，布尔型

Int：基本变量，整型

Float：基本变量，实型

StringTag：引用变量，字符串类型

DiscTag：引用变量，布尔型

IntTag：引用变量，整型

FloatTag：引用变量，实型

如  string s；//定义了一个基本类型的局部变量，字符串类型，变量名为s

stringTag s；//定义了一个引用类型的局部变量，字符串类型，变量名为s

---

第九章  变量定义和管理

## 9.3      变量的属性

变量的属性反映了变量的参数状态、报警状态，历史数据记录状态，比如实型变量“反应罐温度”，可以具有“高报警限”、“低报警限”等属性。当实际温度高于“高报警限”
或低于“低报警限”时，就会在报警窗口内显示报警，而且它们大多是开放的。应用人员可在定义变量时，设置它的部分属性。也可以用命令语言编制程序来读取或设置变量的属性，比如在情况发生变化时，重新设置“反应罐温度”的“高、低报警限”。需要注意的是，有的属性可以被读取或设置，称为“可读可写”型；有的属性只能被读取不能被设置，称为“只读”型；有的属性只能被设置而不能读取，称为“只写”型。从而大大提高了组态的功能。

变量的属性用专门术语称为**“变量的域”**。对每个变量域的引用就是把变量名和域名用“.”号（西文输入状态下的句号）连接起来即可，类似于高级语言（C++）中的“结构”，比如变量“反应罐温度”的报警组名（Group）域，写成“反应罐温度.Group”。

---

第九章  变量定义和管理

### 9.3.1     变量属性类型

变量的属性域越多，表明属性越丰富。不同类型的变量具有不同的域，详解如下：

**1****、基本属性**

基本属性是所有变量都具备的属性，如下所示：

**Name****：**表示变量的名称，字符型，长度为32个字符，只读。

**Comment****：**表示变量的描述内容，字符型，
长度为32个字符，可读可写。

**Value****：**表示变量的数值，根据数据类型的不同，表示为不同（字符串变量的长度为最多127个字符）

**Quality****：**表示变量质量戳的值，32位无符号整型，只读。

**QualityString****：**表示变量质量戳字符串，字符串型，长度为4个字符，只读

**TimeYear****：**表示变量时间戳年的值，16位无符号整型，只读

**TimeMonth****：**表示变量时间戳月的值，16位无符号整型，只读

**TimeDay****：**表示变量时间戳日的值，16位无符号整型，只读

**TimeHour****：**表示变量时间戳小时的值，16位无符号整型，只读

**TimeMinture****：**表示变量时间戳分的值，16位无符号整型，只读

**TimeSecond****：**表示变量时间戳秒的值，16位无符号整型，只读

**TimeMsec****：**表示变量时间戳毫秒的值，16位无符号整型，只读

**TimeZone****：**表示变量时间戳时区的值，16位无符号整型，只读

**TimeDateString****：**表示变量时间戳日期的字符串，长度为32个字符，只读

**TimeTimeString****：**表示变量时间戳时间的字符串，长度为32个字符，只读

**LogEnable****：**控制是否产生历史记录，离散，可读可写

**EventEnable****：**控制是否产生事件记录，离散，可读可写

**ExtendFieldString1****：**表示变量扩展域1，可读可写，字符串类型，最长64个字符

**ExtendFieldString2****：**表示变量扩展域2，可读可写，字符串类型，最长64个字符

**ExtendFieldString3****：**表示变量扩展域3，可读可写，字符串类型，最长64个字符

**ExtendFieldString4****：**表示变量扩展域4，可读可写，字符串类型，最长64个字符

**ExtendFieldString5****：**表示变量扩展域5，可读可写，字符串类型，最长64个字符

**ExtendFieldString6****：**表示变量扩展域6，可读可写，字符串类型，最长64个字符

**ExtendFieldString7****：**表示变量扩展域7，可读可写，字符串类型，最长64个字符

**ExtendFieldString8****：**表示变量扩展域8，可读可写，字符串类型，最长64个字符

**2****、模拟量属性**

模拟量属性域是模拟变量所具备的属性域，如下所示：

**MaxEU****：**最大值，双精度浮点型，可读可写

**MinEU****：**最小值，双精度浮点型，可读可写

**EngUnits** **：**应用单位，长度为8个字符，可读可写

**3****、IO****变量属性**

IO变量属性域是IO变量所具备的属性域，如下所示：

**IOEnable****：**采集使能开关，离散，可读可写

**IORead****：**强制读，离散，可读可写

**IOWrite****：**强制写，离散，可读可写

对于IO模拟量来说，除了上述属性外，还具备如下的属性：

**MaxRAW****：**最大原始值，双精度浮点型，可读可写

**MinRAW****：**最小原始值，双精度浮点型，可读可写

**WriteFeedBackEnable****：**写回馈使能，离散类型，只读

**WriteBeforeValue****：**写下发之前的值，模拟量或字符串或离散量（同值域），只读

**WriteAfterValue****：**写下发之后的值，模拟量或字符串或离散量（同值域），只读

**WriteTime****：**写完成时间，初始为null，字符串类型，只读

**WriteResult****：**写完成结果，初始为null，字符串类型，只读

变量的报警域：关于变量的报警域的介绍请详见第12章报警与事件系统。

引用变量的属性域：引用变量的域和被引用变量的域值应保持一致。

结构变量的域值：结构变量本身没有域值，成员变量参照具体的变量类型和数据类型

---

第九章  变量定义和管理

### 9.3.2     变量属性的使用

在前面章节中已经介绍了一些变量的属性域，变量的属性可以用来在画面上显示，也可以在命令语言中使用。引用变量属性时，可以直接手动输入，也可以通过“变量选择器”来选择，如图9-28所示，在变量选择器中选择相关变量，单击“变量域”列表框，弹出当前选择变量的所有属性列表，选择所需的变量属性即可。

图9-15 选择变量属性

变量属性的引用方法：变量名.属性名（如：\\local\原料罐液位.Alarm）

在脚本编辑器中使用变量属性的方法是：在脚本编辑器中输入变量的名称，然后在变量名后输入“.”号，系统自动列出该变量的属性。如图9-29所示：

图9-16 变量属性列表

其中蓝色标志代表变量的属性，粉色标志代表变量的方法。

---

第九章  变量定义和管理

## 9.4      变量的方法

变量除了具有属性（即变量域）外还具有方法，利用变量的方法可以实现变量值读写及变量属性设置的功能，变量方法如下所示：

1、Ack

long Tag.Ack()

方法功能：

确认变量发生的报警

返回值：

0：方法执行成功

其它值：方法执行失败

举例：

long b=\\local\Stir\_Speed.Ack();

2、Reset

long Tag.Reset()

方法功能：

Reset报警

返回值：

0：方法执行成功

其它值：方法执行失败

举例：

long b=\\local\Stir\_Speed.Reset();

3、Bit

bool Tag.bit(long
bitNo)

方法功能：

取整型变量指定某一位的值。

参数说明：

bitNo：指定的位号，范围是1－32

返回值：

布尔型：

返回指定位号的值

举例：

bool
b=\\local\Stir\_Speed.Bit(3);

4、BitSet

long Tag.bitset(long
bitNo,long value)

方法功能：

为整型变量指定位赋值，该方法适用于只写或读写型变量

参数说明：

bitNo：指定的位号，范围是1—32

value：设定值，0或1

返回值：

长整型，

0：方法执行成功

其它值：方法执行失败

举例：

long
b=\\local\Stir\_Speed.Bitset(3 ,1 );

5、SetFieldValue

long
Tag.SetFieldValue(long Field ,double Value,long Operation)

方法功能：

为指定的变量属性赋值，该方法适用于模拟型或离散型的属性

参数说明：

Field：变量属性的id号，简称域id

Value：设定值

Operation：操作类型

0：不记录该操作事件

1：记录该操作事件，可以通过事件窗口查看该事件的操作历史

返回值：

0：方法执行成功

其它值：方法执行失败

附：SetFieldValue方法适用的属性id表

整型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 15 | LogEnable | 控制是否产生历史记录，离散，可读可写 |
| 16 | EventEnable | 控制是否产生事件记录，离散，可读可写 |
| 19 | Value | 变量数值，根据数据类型的不同，表示为不同（字符串变量的长度为最多127个字符） |
| 20 | MaxEU | 最大值，双精度浮点型，可读可写 |
| 21 | MinEU | 最小值，双精度浮点型，可读可写 |
| 22 | MaxRAW | 最大原始值，双精度浮点型，可读可写 |
| 23 | MinRAW | 最小原始值，双精度浮点型，可读可写 |
| 25 | IOEnable | 采集使能开关，离散，可读可写 |
| 26 | IORead | 强制读，离散，可读可写 |
| 27 | IOWrite | 强制写，离散，可读可写 |
| 32 | AlarmEnable | 变量报警使能域，离散，读写 |
| 56 | HiHiLimit | 高高报警限，双精度浮点，读写 |
| 58 | HiHiEnable | 高高报警使能，离散，读写 |
| 60 | HiHiPriority | 高高报警优先级，16位无符号整型，读写 |
| 61 | HiLimit | 高报警限，双精度浮点，读写 |
| 63 | HiEnable | 高报使能，离散，读写 |
| 65 | HiPriority | 高报警优先级，16位无符号整型，读写 |
| 66 | LoLimit | 低报警限，双精度浮点，读写 |
| 68 | LoEnable | 低报使能，离散，读写 |
| 70 | LoPriority | 低报警优先级，16位无符号整型，读写 |
| 71 | LoLoLimit | 低低报警限，双精度浮点，读写 |
| 73 | LoLoEnable | 低低报使能，离散，读写 |
| 75 | LoLoPriority | 低低报警优先级，16位无符号整型，读写 |
| 79 | RocEnable | 变化率报警使能，离散，读写 |
| 80 | RocPct | 变化率报警限，16位有符号整型，读写 |
| 83 | RocPriority | 变化率报警优先级，16位无符号整型，读写 |
| 88 | DevTarget | 偏差报警限的目标值，双精度浮点，读写 |
| 90 | MajorDevPct | 大偏差报警限，双精度浮点，读写 |
| 92 | MajorEnable | 大偏差报警使能，离散，读写 |
| 94 | MajorDevPriority | 大偏差报警优先级，16位无符号整型，读写 |
| 95 | MinorDevPct | 小偏差报警限，双精度浮点，读写 |
| 97 | MinorEnable | 小偏差报使能，离散，读写 |
| 99 | MinorDevPriority | 小偏差报警优先级，16位无符号整型，读写 |

实型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 15 | LogEnable | 控制是否产生历史记录，离散，可读可写 |
| 16 | EventEnable | 控制是否产生事件记录，离散，可读可写 |
| 19 | Value | 变量数值，根据数据类型的不同，表示为不同（字符串变量的长度为最多127个字符） |
| 20 | MaxEU | 最大值，双精度浮点型，可读可写 |
| 21 | MinEU | 最小值，双精度浮点型，可读可写 |
| 22 | MaxRAW | 最大原始值，双精度浮点型，可读可写 |
| 23 | MinRAW | 最小原始值，双精度浮点型，可读可写 |
| 25 | IOEnable | 采集使能开关，离散，可读可写 |
| 26 | IORead | 强制读，离散，可读可写 |
| 27 | IOWrite | 强制写，离散，可读可写 |
| 32 | AlarmEnable | 变量报警使能域，离散，读写 |
| 56 | HiHiLimit | 高高报警限，双精度浮点，读写 |
| 58 | HiHiEnable | 高高报警使能，离散，读写 |
| 60 | HiHiPriority | 高高报警优先级，16位无符号整型，读写 |
| 61 | HiLimit | 高报警限，双精度浮点，读写 |
| 63 | HiEnable | 高报使能，离散，读写 |
| 65 | HiPriority | 高报警优先级，16位无符号整型，读写 |
| 66 | LoLimit | 低报警限，双精度浮点，读写 |
| 68 | LoEnable | 低报使能，离散，读写 |
| 70 | LoPriority | 低报警优先级，16位无符号整型，读写 |
| 71 | LoLoLimit | 低低报警限，双精度浮点，读写 |
| 73 | LoLoEnable | 低低报使能，离散，读写 |
| 75 | LoLoPriority | 低低报警优先级，16位无符号整型，读写 |
| 79 | RocEnable | 变化率报警使能，离散，读写 |
| 80 | RocPct | 变化率报警限，16位有符号整型，读写 |
| 83 | RocPriority | 变化率报警优先级，16位无符号整型，读写 |
| 88 | DevTarget | 偏差报警限的目标值，双精度浮点，读写 |
| 90 | MajorDevPct | 大偏差报警限，双精度浮点，读写 |
| 92 | MajorEnable | 大偏差报警使能，离散，读写 |
| 94 | MajorDevPriority | 大偏差报警优先级，16位无符号整型，读写 |
| 95 | MinorDevPct | 小偏差报警限，双精度浮点，读写 |
| 97 | MinorEnable | 小偏差报使能，离散，读写 |
| 99 | MinorDevPriority | 小偏差报警优先级，16位无符号整型，读写 |

离散型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 15 | LogEnable | 控制是否产生历史记录，离散，可读可写 |
| 16 | EventEnable | 控制是否产生事件记录，离散，可读可写 |
| 19 | Value | 变量数值，根据数据类型的不同，表示为不同（字符串变量的长度为最多127个字符） |
| 25 | IOEnable | 采集使能开关，离散，可读可写 |
| 26 | IORead | 强制读，离散，可读可写 |
| 27 | IOWrite | 强制写，离散，可读可写 |
| 32 | AlarmEnable | 变量报警使能域，离散，读写 |
| 46 | DiscEnable | 离散变量报警使能，离散，读写 |
| 50 | DiscPriority | 离散变量的报警优先级，16位无符号整型，读写 |

字符串型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 16 | EventEnable | 控制是否产生事件记录，离散，可读可写 |
| 25 | IOEnable | 采集使能开关，离散，可读可写 |
| 26 | IORead | 强制读，离散，可读可写 |
| 27 | IOWrite | 强制写，离散，可读可写 |

6、SetFieldString

long
Tag.SetfFieldString(long Field, string Value, long Operation)

方法功能：

为指定的变量属性赋值，该方法适用于字符串类型的属性

参数说明：

Field：变量属性的id号，简称域id

Value：设定值

Operation：操作类型

0：不记录该操作事件

1：记录该操作事件，可以通过事件窗口查看该事件的操作历史

返回值：

0：方法执行成功

其它值：方法执行失败

附：SetFieldString方法适用的属性id表

整型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 2 | Comment | 变量描述，字符型， 长度为32个字符，可读可写 |
| 17 | ExtendFieldString1 | 变量扩展域1，长度为16个字符，可读可写 |
| 18 | ExtendFieldString2 | 变量扩展域2 |
| 24 | EngUnits | 应用单位，长度为8个字符，可读可写 |
| 59 | HiHiString | 高高报警文本，长度为128个字符 |
| 64 | HiString | 高报警文本 |
| 69 | LoString | 低报警文本 |
| 74 | LoLoString | 低低报警文本 |
| 81 | RocString | 变化率报警文本 |
| 93 | MajorString | 大偏差报警文本 |
| 98 | MinorString | 小偏差报警文本 |

实型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 2 | Comment | 变量描述，字符型， 长度为32个字符，可读可写 |
| 17 | ExtendFieldString1 | 变量扩展域1，长度为16个字符，可读可写 |
| 18 | ExtendFieldString2 | 变量扩展域2 |
| 24 | EngUnits | 应用单位，长度为8个字符，可读可写 |
| 59 | HiHiString | 高高报警文本，长度为128个字符 |
| 64 | HiString | 高报警文本 |
| 69 | LoString | 低报警文本 |
| 74 | LoLoString | 低低报警文本 |
| 81 | RocString | 变化率报警文本 |
| 93 | MajorString | 大偏差报警文本 |
| 98 | MinorString | 小偏差报警文本 |

离散型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 2 | Comment | 变量描述，字符型， 长度为32个字符，可读可写 |
| 17 | ExtendFieldString1 | 变量扩展域1，长度为16个字符，可读可写 |
| 18 | ExtendFieldString2 | 变量扩展域2 |
| 47 | DiscOffString | 离散变量关断报警文本，读写 |
| 48 | DiscOnString | 离散变量开通报警文本，读写 |

字符串型变量：

|  |  |  |
| --- | --- | --- |
| 属性id | 属性名称 | 属性含义 |
| 2 | Comment | 变量描述，字符型， 长度为32个字符，可读可写 |
| 17 | ExtendFieldString1 | 变量扩展域1，长度为16个字符，可读可写 |
| 18 | ExtendFieldString2 | 变量扩展域2 |
| 19 | Value | 变量数值，根据数据类型的不同，表示为不同（字符串变量的长度为最多127个字符） |

---

第九章  变量定义和管理

## 9.5      变量域函数

除了上节介绍的变量的方法外，KingSCADA还提供了用来操作变量域值的函数，如获取变量域值、设置变量域的值、将变量的域值保存到报警事件库的Operate表等。下面将对这些函数一一进行介绍。

1、Bool SetTagField(string TagName, string FieldName,
string Value)

功能：设置变量域的值。

参数：

TagName：变量名称

FieldName：变量域名称

Value:
变量域的值

返回值：布尔型。True：设置成功；False：设置失败。

 

2、String GetTagField(string TagName, string
FieldName)

功能：通过变量域名称获取变量域值函数。

参数

TagName：变量名称

FieldName：变量域名称

返回值：字符串，把变量域值转换成字符串返回。

 

3、double GetTagFieldFloat(string TagName,
string FieldName)

功能：通过变量域名称获取浮点型变量域值函数。

参数

TagName：变量名称

FieldName：变量域名称

返回值：浮点型，返回浮点型变量域值。

 

4、long GetTagFieldInt(string TagName, string
FieldName)

功能：通过变量域名称获取整型变量域值函数。

参数

TagName：变量名称

FieldName：变量域名称

返回值：整型，返回整型变量域值。

 

5、Int SaveTagFiledValueToAEDB1(string strTagName，string strTagFieldName，string
strOperateText)

功能：

保存变量的任何域值到AEDB的Operate表里。

参数：

strTagName：变量名

strTagFieldName：变量域名，为空时，其值默认为"Value"。

strOperateText：描述信息。

返回值：

整型

0 成功，-1 未知错误，-2 变量名参数错误，-3 域名参数错误，-4
向数据库添加记录失败

***注：******保存变量任何域值到报警事件库的Operate******表，其中EventType******字段的数据为"******自定义"******。***

---

第九章  变量定义和管理

## 9.6      变量组

当应用中拥有大量的变量时，会给开发者查找变量带来一定的困难，为此KingSCADA提供了变量分组管理的方式。即按照应用的需要将变量放到不同的组中，这样在修改和选择变量时，只需到相应的分组中去寻找即可，缩小了查找范围，节省了时间。并且它对变量的整体使用没有任何影响。

---

第九章  变量定义和管理

### 9.6.1     新建/编辑变量组

在工程设计器树形目录下选择“建点”→“数据词典”选项并单击鼠标右键，在弹出的右键菜单中执行“新建变量组”命令，在弹出的对话框中为变量组命名，如车间1变量组，单击“确定”按钮后该变量组出现在“数据词典”的下面，如图9-30所示：

图9-17 新建变量组

变量组建立完成后，可以在变量组下直接新建变量，在该变量组下建立的变量属于该变量组。变量组中建立的变量可以在系统中的数据词典中全部看到。在变量组下，还可以再建立子变量组，如图9-31所示。属于子变量组的变量同样属于上级变量组。

图9-18 建立子变量组

选择建立的变量组，单击鼠标右键，在弹出的右键菜单中执行“编辑选中的变量组”可修改变量组。

---

第九章  变量定义和管理

### 9.6.2     在变量组中增加变量

变量组建立完成后，就可以在里面增加变量了，增加变量有两种方法。

**方法一：**选择建立的变量组，单击鼠标右键，在弹出的右键菜单中执行“新建变量”命令，即可建立该变量组中的变量。

**方法二：**在数据词典中或某一变量组中选择一个或多个变量，单击鼠标右键，在弹出的右键菜单中执行“添加到变量组”命令，在弹出的变量组选择框中选择目标变量组后，单击“确定”按钮，在变量被添加到目标变量组中，但是在数据词典中该变量还存在，而在原变量组中该变量将消失。

在变量分组完成后，使用时，只需选择相应的变量组即可。变量的引用不受变量组的影响，所以变量可以被放置到任何一个变量组下。

---

第九章  变量定义和管理

### 9.6.3     从变量组中删除变量

如果不需要在变量组中保留某个变量时，可以选择从变量组中删除该变量，也可以选择将该变量移动到其它变量组中。从变量组中删除的变量将不属于任何一个变量组，但变量仍然存在于数据词典中。

选择变量组，在右侧“内容显示区”中选择该变量组中欲删除的变量，单击“移除”按钮，则该变量从当前变量组中消失。

---

第九章  变量定义和管理

### 9.6.4     如何删除变量组

当不再需要变量组时，可以将其删除，删除变量组前，首先要保证变量组下没有任何变量存在，另外也要先将子变量组删除。

选择欲删除的变量组，单击鼠标右键，在弹出的右键菜单中执行“删除选中的变量组”命令即可。

---

第九章  变量定义和管理

## 9.7      检索组

在开发KingSCADA项目过程中，建立了大量变量之后，涉及到变量的属性修改，并且要修改的变量的个数是若干个，一般情况这些变量都会有些共性的信息，这时候需要通过条件将这些变量检索出来进行批量编辑，可大大提供编辑的效率。

为了能够快速在数据词典中查找一批变量，并对这些变量进行统一的编辑，在KingSCADA中提供一种“检索组”，这种“检索组”可定义查询条件，并将符合条件的变量全部显示到检索分组下。通过检索分组可快速的筛选某些变量。

---

第九章  变量定义和管理

### 9.7.1    新建/编辑检索组

在工程设计器目录下选择“建点”中“数据词典”或“某个检索组”选项的右键菜单，如下图所示

图9-19

图9-20

点击“新建检索组”，进入检索定义界面，可定义“检索组名称”和检索条件，检索条件可以是一个或若干个条件的组合：

图 9-21

添加或编辑条件项，单击“确定”按钮后该检索组出现在“数据词典”的下面，如图9-22所示：

图9-22 新建检索组

**条件输入说明：**

**操作符**包括AND（与）、OR（或）；

**检索字段**为变量属性域，参见9.3.1；

**条件判断符：**

1）离散型，提供==，<>

2）数值型，提供==，>，>=，<，<=，<>

3）字符串型，提供==，<>，INCLUDE（包含）、UNINCLUDE（不包含），支持模糊查询，如tag\*

4）报警组，提供IN，表示报警组的包含关系

**条件值：**

1）离散型，条件值选项为true代表1，false代表0

2）数值型，整数或浮点数

3）字符串型，字符串输入，报警组除外

**检索字段条件值的特殊说明：**

1）报警组条件值需要从报警组列表中选择，当定义条件为报警组时，选择按钮“”有效。

2）离散型检索字段：提供true和false选择项；

3）Type：变量类型，提供基本，结构，引用选择项

4）SubType：变量子类型，提供内存，IO选择项

5）DataType：数据类型，提供离散、字符串、整型、浮点型选择项

6）SecurityZone：安全区，提供安全区选择项

7）HisRecMode：历史记录方式，提供无、数据改变、间隔、备份、每次采集

8）RocTimeUnit：变化率报警时间单位，提供秒、分、时选择项

**例1：**检索出变量名包含“压力”的IO变量，统一修改变量报警限值，可以输入以下条件：

图9-23

检索结果如下图所示：

图9-24

**例2：**检索出历史记录方式为数据改变或间隔的IO变量，统一历史记录方式为每次记录采集，可以输入以下条件：

图9-25

检索结果如下图所示：

图9-26

**注：优先级说明：括号优先级最高、其次是AND、最低位OR**

选择建立的检索组，单击鼠标右键，在弹出的右键菜单中执行“编辑检索组”可修改检索组条件信息。

---

第九章  变量定义和管理

### 9.7.2    刷新检索组

在工程设计器树形目录下选择“建点”→“数据词典”→“检索组”选项并单击鼠标右键，在弹出的右键菜单中执行“刷新检索组”命令，即可按照设定条件项刷新出变量。

---

第九章  变量定义和管理

### 9.7.3    删除检索组

在工程设计器树形目录下选择“建点”→“数据词典”→“检索组”选项并单击鼠标右键，在弹出的右键菜单中执行“删除检索组”命令，进去确认画面，如下图所示，

图9-27 删除检索组

点击“是”，检索组被删除，点击“否”，检索组不被删除。

---

第九章  变量定义和管理

### 9.7.4    从检索组中编辑变量

如果需要对某些变量的共有属性进行编辑时，可以在检索组中批量选择变量，视图工具栏或右键菜单选择编辑，修改共有属性，点击确定。

**注：在变量属性窗口中，变量名一栏中显示选择的变量，并灰掉（选择了多个变量或变量已经在工程中使用）不能够进行更改。**

---

第九章  变量定义和管理

### 9.7.5    从检索组中删除变量

如果不需要在数据词典中保留某些变量时，可以通过检索组检索出这些变量并进行删除，选择该变量，视图工具栏或右键菜单选择删除，弹出删除提示框，如下图所示：

图9-28删除变量

点击是，如工程中未使用此变量，直接删除；如工程中已使用此变量，弹出提示，如下图所示：

图9-29变量已使用

点击否，变量未被删除。

---

第九章  变量定义和管理

### 9.7.6    从检索组中导出变量

如果需要对检索出的变量导出时，可以在检索组中批量选择变量，视图工具栏或右键菜单选择导出，将变量导出到文件中，支持xls、xlsx、csv、xml后缀名的文件。

---

第九章  变量定义和管理

## 9.8      快速添加变量

“服务端应用”中的变量，具有报警和历史存储域，可以是内存变量，也可以是IO变量。IO变量的IO属性只有“访问名称”，如果这个属性域为空，则自动成为内存变量。

通过导入其他应用（如IOServer应用）部分变量（一个或一批），达到快速建立变量的目的；在导入的过程中保留服务端应用变量需要的域，将不需要的域属性抛弃，对于没有的域属性自动填充默认值；

 

图9-30 数据词典树型目录

在上图所示对话框中，点“数据词典”或者“变量组名称”均可进入到如下图所示工作区：

图9-31快速添加命令

在上图所示界面中，单击工具栏或右键菜单的“快速添加”项，进入到“快速添加IO变量”的对话框中。如下图所示：

 

图9-32快速添加IO变量

数据源类型：只有“IOServer应用程序”一个选项。选择IOServer应用，“数据源”下自动列出所有的已添加的IOServer应用的站点名称，选择其中的一个。当选择的数据源找不到或已经更名时，鼠标离开“数据源”下拉列表框后，会弹出提示框，单击“确定”按钮后，左侧的树型目录区只显示站点名称的节点；如果选择的数据源正常的话，左侧的树型目录区将定位到“用户变量”这个节点。

在“过滤条件”中根据需要选择过滤条件。过滤条件包括变量名，变量类型、关联设备。当选择变量名时，在后面的编辑框中输入要查找变量名称（有效长度为64个字符），后面的编辑框中支持通配符的输入，其中，\* 代表任意字符串，？代表任意单个字符。当选择变量类型或关联设备时，后面的编辑框变为可选下拉列表，列出可选的变量类型或关联设备。这里的变量类型为IOServer服务器中所有的变量类型，关联设备为选中的IOServer服务器中已建立的所有的设备名称。

由于过滤条件非常灵活，这里只作空校验。当过滤条件为空时，会提示：“过滤条件不能为空！”。

完成过滤条件的选择后，单击后面的“过滤”按钮。左侧显示IOServer应用的变量树结构，变量树包括系统变量、设备变量和用户变量（下面显示变量组）三种类型；右侧显示符合过滤条件的变量列表。单击“用户变量”，右侧会显示符合条件的所有用户变量；选择某个变量组时，右侧会显示该组下符合过滤条件的变量。

**1****、单个添加变量**

勾选要添加的变量（可以勾选不同变量组下的）并选择变量的变量域，这里的变量域是IOServer中变量的所有域，变量默认为不勾选。若在把勾选的变量添加为“新服务应用”中的变量时想将名称中某个字符串替换为另外的字符串，或者加前辍，后辍，可通过下面对话框，如图9-33所示，进行替换或修改即可。

变量名增加前、后缀和变量名字符串替换不能同时使用。

前缀、后缀、字符串替换文本做无效字符校验，无效字符包括：,;:+-\*/%&!~|^<>={}[]().'"\
?和空格。

² 
前缀和后缀本身不能包含非法字符：,;:+-\*/%&!~|^<>={}[]().'"\ ?。

² 
前缀、后缀的有效长度为16个字符

² 
前缀不能以数字开头

² 
前缀+原变量名+后缀，总长度不能超过64个字符

² 
前缀+原变量名+后缀不能与已存在的对象重名（这里的对象包括所有变量名和报警组名）

² 
组的前缀、后缀的有效长度为4个字符。

² 
前缀+组名+后缀不能与已存在变量组名重名。

² 
前缀+组名+后缀，总长度不能超过32个字符

设置好上面的条件后，单击“确定”按钮，将会把成功通过校验的变量添加进来，并弹出如下图9-35所示的对话框，列出添加错误的变量（变量重名），处于变量重名状态的变量的“可添加”状态为“NO”。“添加控制”对话框中列出的变量信息包括：可添加状态、名称、类型、描述、IO访问名称。

图9-33 添加控制

变量如出现与现有变量重名的情况时会有2种方式处理——“替换”和“重命名”。

“替换”：选中下图的某个或某些重名变量，单击“替换”按钮，则变量的“可添加”状态会由“NO”变为“YES”，单击“确定”按钮即可以添加成功（不同类型的变量不能替换，即“替换”按钮灰显不可用）。

“重命名”会弹出对话框要求用户输入新变量名称。如下图所示：

图9-34

新名称：变量的新名称，不能为空，默认为系统根据重名机制自动生成的名称（即原名称+序号，序号为1,
2, 3, 4, …），做重名、空和非法字符校验，并且第一个字符不能为数字，64字符长。非法字符包含空格字符和如下28个无效字符：,:;+-\*/%&!~|^<>={}[]().`’
”\?。单击“确定”按钮，校验输入的名称是否为空、是否与其它变量重名，是否含有非法字符，并给出相应的提出。

图9-35

图9-36

 

图9-37

变量重命名成功后，变量的可添加状态会由“NO”变为“YES”。

处理好处于添加错误状态的变量，并勾选需要添加的变量，单击“添加”按钮添加变量（经过替换处理的变量会用添加变量的属性替换原有变量的属性），添加成功的变量将会在变量列表中显示。添加变量结束后，会有添加变量的报告栏，提示添加成功的变量数量、添加失败的变量数量及名称，以及添加失败的原因。

图 9-38

**2****、带组添加变量**

选中树形结构中要添加的变量组，变量组下的所有变量自动被选中，允许用户根据实际需要手动取消不需要的变量。带组添加会连同选中的变量组的结构一起添加到数据词典中已经选中的变量组下。数据词典中显示的组结构是所有被选中的变量组的结构。例如：变量组ff前面打勾，右侧显示ff变量组中的所有变量， ff变量组节点还有子节点sdg，如果sdg变量组下还有子组ad，而是在添加组时只选了ff组和ad组 ，没有选sdg组，右侧只显示ff组和ad组下的所有变量。那么添加后，在数据词典中相应的位置显示ad变量组和ff变量组。而且ad变为ff变量组的子组。添加的变量也只有ff组和ad组下的所有变量。

带组添加变量时，单击图9-29的“确定”按钮后，添加的变量组名将会和在数据词典中的同一级变量组名进行校验。只校验添加的变量组的第一级变量组节点名称。如果同级变量组有相同的组名，会自动给要添加的变量组名后面加序号，该序号是从1开始的自然数1,2,3，……n。例如：如果要添加ac变量组和它的子变量组节点，那么只校验ac变量组和数据词典中与它同级的变量组名称是否同名，不校验ac子变量组的名称是否有同名的变量组。如果数据词典中存在与ac同级同名的变量组，直接把ac变量组的名称变为ac1。校验完变量组名后进行变量名校验，变量名校验规则与单个添加变量时一样，不再详细说明。

---

第九章  变量定义和管理

## 9.9      批量添加变量

对于变量点数很多的大型应用来说，批量添加变量的功能就显得非常重要了，同一时刻增加上百点不仅使开发人员从繁琐的工作中解脱出来同时也大大缩短了应用开发周期。在KingSCADA中实现该功能的操作如下：

**第一步：**在工程设计器中打开数据词典，如图9-39所示。

图9-39 数据词典

**第二步：**在数据词典中新建一变量，如speed，选中该变量，此时“批量添加”命令变为有效，单击该命令弹出对话框，如图9-40所示：

图9-40 变量批量添加设置

**批量数量：**设置批量增加的数量，最大值为50000

**前缀/****后缀：**设置变量名的前缀和后缀

**步长：**设置相邻变量之间的数据间隔

例如：对于上述新建的speed变量，该对话框设置如下：

图9-41批量添加变量例子

单击“确定”按钮后，效果如下图9-42所示：

图9-42批量添加变量效果

对于变量描述的批量添加功能与变量名称批量添加功能相似，在此不再赘述。

**第三步：**对于IO变量，当按下“批量添加”命令后弹出对话框，如下图所示：

 

图9- 43  IO变量批量添加对话框

IO设置中“IO变量名”是指IO变量中“访问名称”中的变量名。单击“确定”按钮将校验由站点名称、新的IO变量名、变量域组成的访问名称是否有效。如果无效会弹出如下图提示对话框。

图9-44 无效提示框

如果有效的话，会在变量列表中列出新添加的变量。批量添加的变量的其它属性都和原来选中的那个变量的属性相同。

---

第九章  变量定义和管理

## 9.10批量编辑变量

为了修改多个变量的共同属性，例如调整几个模拟量的描述信息为“车间1”，最大值调整为1000，可以同时选中这几个变量后单击鼠标右键，在弹出的右键菜单中执行“编辑”命令或单击数据词典窗口上的“编辑”选项，如下图所示：

图9- 45 变量批量编辑

上图中虚线框中的变量为焦点变量（即Tag4），此时弹出焦点变量属性对话框，修改完描述、最大值并确定后，修改的属性被传播到其他变量的属性中，即完成了同时修改多个变量属性的功能。

***注意：***

***当选中的变量同时包含离散量、模拟量、字符串量、结构变量、引用变量的两种或以上时，只有共同的属性并被修改过的属性才会从焦点变量传播到其他选中变量。***

***相同模板的多个结构变量成员批量编辑时，修改多个成员属性并确定后，传播会根据成员顺序，依次传播属性；非相同模板的结构变量，按成员批量编辑功能无效。***

---

第九章  变量定义和管理

## 9.11变量的其它操作

---

第九章  变量定义和管理

### 9.11.1   变量的复制、剪切与粘贴

同一应用内部复制、粘贴变量时，系统会严格复制原变量，并在原变量名的基础上通过添加数字后缀方式，自动给新变量起一个唯一的名字。

在不同应用之间复制变量时，一些与其他应用对象存在关联的变量属性会被复位，例如报警组属性，如果原变量报警组为A，而复制后粘贴到其他应用，则新生成的变量的报警组将自动复位成RootNode。

---

第九章  变量定义和管理

### 9.11.2   变量的导出/导入

KingSCADA应用中的变量可以导出到xml、xls、xlsx、csv格式的文档中，也可从这四个格式的文档中导入到KingSCADA应用中，操作方法如下：

***注：关于变量的导入和导出需要注意一下事项。***

***1******、KingSCADA******只支持Excel2003******及之后的版本，\*.xls******为Excel2003******版本文件，\*.xlsx******为Excel2007******版本的文件，对于低版本的Excel******不再支持。***

---

第九章  变量定义和管理

#### 9.11.2.1   变量导出

在工程设计器中打开数据词典，如下图所示：

图9-46 数据词典

在数据词典中选择预导出的变量，此时“导出”命令有效，单击此命令，在弹出的对话框中输入文件名称及文件类型即可。

变量导出时带有对应的变量组信息，导入的时候可以自动创建变量组。

图-变量组相关信息

---

第九章  变量定义和管理

#### 9.11.2.2   变量导入

在图9-46中单击“导入”命令，可将xml、xls、xlsx、csv格式文件中保存的变量导入到数据词典中，如果预导入的变量与数据词典中现有的变量重名，系统会弹出如下图所示的对话框：

图9-43变量导入控制对话框1

在该对话框中选中某一变量，此时“替换”按钮和“重命名”按钮有效。单击“替换”按钮用导入的变量替换数据词典中的变量或者单击“重命名”按钮为导入的变量重新命名，则“可添加”列的值由No变为Yes，如下图所示：

图9-48变量导入控制对话框2

此时再次单击9-48中的“添加”按钮将该变量添加到数据词典中。或者在图9-48中同时选中多个变量，此时“替换”按钮有效，单击“替换”按钮用添加的变量替换数据词典中的变量，然后单击“添加”命令，选中的多个变量添加到数据词典中。如果添加操作出现问题的话，系统自动弹出添加报告对话框，如下图所示：

图9-49导入报告对话框

在图9-49中单击“另存”按钮将导入报告保存为xml格式的文件，便日后查看。

在从Excel格式导入到KingSCADA的数据词典中时，软件要对Excel格式数据进行严格的校验，只有校验通过后才能导入到数据词典，否则将拒绝导入。因此在导入之前请仔细检查尽可能保证数据的正确，以节约时间，减少修改和导入反复的次数，建议在导入前先备份应用，以备出现异常情况时恢复到导入前状态。以下是对变量导入导出的简要说明，请在执行操作前仔细阅读：

1、STRUCT\_TEMPLATE和STRUCT\_TAG是对词典中导出的复合结构变量的框架说明，请不要编辑这两页。STRUCT\_TEMPLATE和STRUCT\_TAG的成员变量具体信息保存在后续的基本变量类型页中，成员变量的具体属性可参照约束规则做相应编辑。

2、REF\_TAG是对引用类型变量的简单说明。

3、最后的八张变量页分别对应KingSCADA的八种基本变量类型：MEM\_DISC、MEM\_INT32、MEM\_FLOAT、MEM\_STRING、IO\_DISC、IO\_INT32、IO\_FLOAT和IO\_STRING。您可以在这八张变量页中找到导出的基本类型变量的基本类型成员变量。每张基本类型页的第一列和第三列对于导入过程很重要，强烈建议不要修改它们。其它列的数据可参照约束规则做相应编辑。

4、导出表单中的每一列都对应导出对象的一个属性，它们都有自己特定的数据类型，如：字符串，整型，离散型等等。如果对表单数据进行修改或添加，请按照各自的类型来操作。

约束规则：

（1）模板成员类型有效值：整型、实型、离散型、字符串、其它模板类型名称。

（2）名称：由字母、数字、汉字组成，但不能以数字开头。有效长度为小于64。

（3）是否保存值：有效值为是、否。

（4）是否保存参数：有效值为是、否。

（5）报警组：KingSCADA中定义的内容。

（6）优先级：1-999。

（7）低低（低、高、高高、关、开、）报警文本：有效长度为小于128。

（8）变化率单位：有效值为时（2）、分（1）、秒（0）。

（9）扩展域1、扩展域2：有效长度为小于64。

（10）历史记录方式：有效值为不记录（0）、变化记录（1）、每次采集记录（2）、定时记录（3）。

（11）是否生成操作事件：有效值为是、否。

（12）安全区：KingSCADA中定义的内容。

（13）注释：有效长度为小于32。

（14）是否产生报警：有效值为是、否。

（15）离散变量报警类型：有效值为开、关。

（16）数据类型：I/O整型和I/O实型。有效值为BIT（1）、BYTE（2）、SHORT(3)、USHORT(4)、BCD（5）、LONG（6）、LONGBCD（7）、FLOAT（8）和DOUBLE（9）。I/O离散型变量的有效值为BIT（1）。I/O字符串的有效值为STRING（10）。

（17）读写属性：有效值为读写（1）、只读（0）、只写（2）。

（18）是否允许其他应用程序访问：有效值为是、否。

（19）转换方式：有效值为线性（1）、取反（2）、开方（4）、查非线性表（16），加累计（32）和差累计（48）。查非线性表，加累计和差累计是对开方转换方式的具体诠释。您可以通过在转换方式栏输入一个不同转换方式代表数字（括号中所指）的和值来实现若干转换方式的叠加，但这种叠加不能任意搭配。

变量导入失败可考虑以下几种原因：

1）如果原来的数据词典里有同名的变量，并且已经被引用了，那么就不能导入替换原来的变量，可以在导入时更改变量名导入。

2）如果把一个内存变量改为IO变量再导入，如果这个内存变量已经被引用了，那么改后的IO变量就不能导入替换原来的内存变量，可以在导入时更改变量名导入。

3）导入变量时，如果有IO变量，必须要把IOServer打开才行。

---

第九章  变量定义和管理

#### 9.11.2.3   查看导入报告

在图9-48中单击“查看导入报告”选项可以查看保存的导入报告。

---

第九章  变量定义和管理

### 9.11.3   清除未使用的变量

在工程设计器的树型目录中选中“建点”→“数据词典”选项，单击鼠标右键，在弹出的右键菜单中执行“清除未用变量”命令，如图9-50所示：

图9-50清除未用变量选择菜单

执行此命令弹出“删除未使用变量”对话框，如图9-51所示：

图9-51清除未使用变量窗口

在该窗口中列举出当前应用中未使用的所有变量，选中单个变量或者单击“选择全部”按钮后单击“确定”按钮删除选中的变量或删除所有变量，被删除的变量从数据词典中消失。

---

第九章  变量定义和管理

### 9.11.4   更新引用计数

在系统维护中，如版本升级，变量引用计数可能得不到更新，造成有些变量不能删除、编辑名称等问题，此时可使用本功能，强制变量引用计数复位后重新计算，以确保引用逻辑正确。

---

第九章  变量定义和管理

### 9.11.5   变量的右键菜单命令

在数据词典中选中一变量（系统变量除外），单击鼠标右键，弹出右键菜单，如下图所示：

图9-52变量的右键菜单

菜单命令及功能描述如下：

|  |  |
| --- | --- |
| 命令 | 功能描述 |
| 新建 | 使用此命令新建变量 |
| 编辑 | 使用此命令重新编辑选中的一个或多个变量 |
| 删除 | 使用此命令删除选中的一个或多个变量，如果选中的变量被应用的任何地方引用的话，将无法删除 |
| 添加下一个 | 在数据词典中选中某一变量，单击此命令系统自动添加一个变量，该变量继承了选中变量的所有属性，唯有变量名后面多了一个数字，继续单击此命令数字将不断增加 |
| 批量添加 | 使用此命令可以实现在数据词典中批量添加变量的功能，操作过程请详见9.8节 |
| 拷贝/粘贴 | 使用此命令可以实现在同一应用内或不同应用之间变量的拷贝和粘贴操作 |
| 导出/导入 | 使用此命令导出选中的一个或多个变量，操作过程请详见9.10节 |
| 重命名 | 使用此命令重新命名选中的一个或多个变量，对于多个变量只允许同时为多个变量添加相同的前缀或后缀 |
| 新建数据模型 | 使用此命令可以新建一个数据模型，并将选中的一个或多个变量自动添加到该数据模型行，作为数据模型中的变量，数据模型的详细介绍请参见第13章模型 |
| 添加到变量组 | 使用此命令可以将选中的一个或多个变量添加到指定的变量组中，变量组的详细介绍请详见9.6节 |

   
数据词典窗口上面的命令按钮可以实现与右键菜单相同的功能。