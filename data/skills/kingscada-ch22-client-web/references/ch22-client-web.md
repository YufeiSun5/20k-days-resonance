第二十二章 web发布及IE浏览

# 第二十二章 web发布及IE浏览

---

第二十二章 web发布及IE浏览

## 22.1         功能介绍

KingSCADA软件采用C/S架构，在一个工业现场可能会部署多个KingSCADA客户机，客户机不仅数量众多，而且分布在不同的监控地点，所以KingSCADA客户机客户端软件和应用的安装、维护与升级工作量较大。

为了减轻用户安装和维护客户机的工作量，降低KingSCADA的使用维护成本，我们为用户提供了一种能统一管理、自动部署KingSCADA客户机客户端软件和应用的技术手段，即基于C/S架构的Web化集中管理技术。

Web化是指将KingSCADA
客户端应用和客户机软件以网页程序形式存放在网络的Web服务器上，各客户机只须运行本机自带的网页浏览器软件，浏览Web服务器上的网页文件，网页文件里的下载插件即可将网站上的客户端应用包和客户机组件自动下载，安装，并自动加载客户端运行。

为此，我们提供一个客户端打包和发布工具ClientPack和一个下载插件KxClientDownload.ocx。

ClientPack的主要功能：

① 帮助用户将客户机软件和应用打包。

② 创建一个嵌有下载插件、设置了下载插件各种属性的网页文件

③ 创建一个包含有网页文件的，能下载和运行客户机软件和应用的网站文件。

③ 将网站程序部署到指定Web服务器某站点上。

KxClientDownload.ocx是一种会随着IE浏览器的启动自动执行的插件.，当用户浏览到嵌有下载插件的Web站点网页时，下载插件会自动完成从Web网站下载、安装客户端软件和客户端应用，并加载客户端运行组件和应用的一系列工作。

令外，KxClientDownload.ocx还可以作为第三方软件中的控件使用，用来加载其它的ocx控件或WEB网址，关于该控件的属性和事件将会在该章的最后进行讲解。

---

第二十二章 web发布及IE浏览

## 22.2         web发布及IE浏览功能配置操作

  
KingSCADA web发布必须使用客户端工程，如何使用服务器工程快速生成客户端工程，参见19.3章节中的方法。

---

第二十二章 web发布及IE浏览

### 22.2.1   IIS配置

在进行Web发布前，需要在Web服务器上安装IIS（即Internet信息服务）组件，安装过程如下：

点击Windows开始菜单，选择“控制面板”，在打开的控制面板中选择“添加或删除程序”选项

1、Windows10/11
IIS的安装

打开“控制面板--à程序和功能--à启用或关闭Windows功能”，点击 “启用或关闭Windows功能”弹出windows功能对话框，如图所示：

图22-1 IIS安装IIS服务组件

在上图中选择“Internet Information
Services”选项，点开前边“+”，列出IIS相关的配置项，**勾选****web****管理工具和万维网服务—****应用程序开发功能下所有跟iis****相关的配置项**，选择好后单击“确定”按钮，系统开始安装IIS组件，几分钟后IIS即可安装完成。配置项如下图所示：

 

图22-2 IIS安装配置

2、  Windows Server 2008 IIS的安装

点击“控制面板”->“管理工具”->“服务器管理器”，在出现的“服务器管理器”界面中，选中“角色”后，在显示的“角色”界面单击“添加角色”，弹出“添加角色向导”对话框。单击“下一步”按钮，打开如图22-6所示界面。

图22-3 Internet信息服务管理器配置

勾选“Web服务器（IIS）”后，单击“下一步”按钮。

图22-4 Internet信息服务管理器配置

在“角色服务”中，勾选“管理工具”中的“IIS管理控制台”、“IIS管理脚本和工具”、
“管理服务”、“IIS6管理兼容性”选项；在“Web服务器”的 “应用程序开发”
中勾选“ASP.NET”、“.NET扩展性”、“ASP”选项，选择好后单击“下一步”按钮，在随后弹出的对话框中选择“安装”按钮，系统开始安装IIS组件，几分钟后IIS即可安装完成。

图22-5 Internet信息服务管理器配置

3、  Windows Server 2016 IIS的安装

打开“控制面板--à程序和功能--à启用或关闭Windows功能”，点击“启用或关闭Windows功能”弹出服务管理器.仪表板，单击“添加角色和功能”，弹出“添加角色向导”对话框。单击“下一步”按钮，打开如图22-6所示界面。

图22-6 Internet信息服务管理器配置

单击“下一步”按钮，一直到“服务器角色”页面，勾选“web服务器（iis）-àweb服务器-à应用程序开发”和“管理工具”，全部勾选上，然后点击确定，系统开始安装IIS组件，几分钟后IIS即可安装完成。Iis配置界面如下图所示：

 

图22-7 Internet信息服务管理器配置

---

第二十二章 web发布及IE浏览

#### 22.2.1.1  客户端工程发布

以上准备工作完成后就可以进行客户端应用的打包和发布了，下面介绍局域网中进行IE浏览时，客户端工程发布配置过程。

如果是在广域网中进行IE浏览，则参见22.3章节跨网关浏览。

---

第二十二章 web发布及IE浏览

#### 22.2.1.2  Client打包和发布工具

启动Client应用打包和发布工具，启动方法有两种方式：

第一种方式通过客户端工程开发系统下“附加工具”中内置“客户端打包工具”打开，，如下图所示：

图22-9 附加工具中启动客户端打包工具

  打开“客户端打包工具”后，不需要再选择Client应用路径了，系统已经自动将本应用所在硬盘路径添加上了。采用第一种方式打开，配置过程可以直接跳过
“**1****、选择Client****应用”**中描述内容，直接配置“**2****、选择发布画面**”中内容。

**注：**服务端工程开发系统下“附加工具”中无内置“客户端打包工具”。

第二种方式通过开始菜单kingSCADA目录下，工具à客户端打包工具，如下图所示：

图22-10 启动客户端应用打包和发布工具

单击“Client打包和发布工具”命令，弹出对话框，如图所示：

图22-11 客户端应用打包和发布界面

**1****、选择Client****应用：**

单击按钮，在弹出的文件浏览器中选择已存在的Client应用，应用的扩展名为.kcapp，打开应用的对话框，如下图所示：

图22-12 打开Client应用界面

**2****、选择发布画面：**

在“应用画面”列表框中选择要发布的画面，然后单击按钮将选中的画面移动到“选中画面”列表框中，同样可以单击按钮将已选中的画面取消发布，也可以通过双击画面的方法添加或取消画面发布。

**3****、选择浏览时的初始画面：**

在“选中画面”列表框中选择一个或多个画面作为客户端浏览时的初始画面，即打开IE浏览时首先将显示的画面，如图所示，“ggg”画面被设置为初始画面。

图22-13 选择初始画面

**4****、打包、建立Web****站点设置：**

应用发布分自动发布和手动发布两种情况。当使用自动发布时，客户端应用会被打包到相应的输出路径中并发布到在IIS上自动创建的虚拟目录中，实现应用的Internet/Intranet访问和浏览。当使用手动发布时，客户端应用只被打包到相应的输出路径中, 并不会在IIS上创建虚拟目录，需要手动在IIS上建立一个与“客户端应用打包输出路径（本地）”下的打包应用相对应的虚拟目录，才能实现应用的IE访问和浏览。

**情况一：**自动发布。方法如下：

单击“客户端应用打包输出路径（本地）”文本框后面的按钮，在弹出的文件浏览器中选择用来存储客户端应用打包文件夹的路径，选择的路径信息将会显示在“客户端应用打包输出路径（本地）”文本框里。客户端应用打包文件夹的路径选择需要注意以下2点：

（1）不要选择客户端应用所在的目录。

（2）因为操作系统的用户目录有访问权限的问题，所以不能选择用户目录、我的文档、桌面这类路径，否则客户端下载会失败。

在“虚拟目录”文本框中输入任一虚拟目录的名称（如：web），选中“配置本机IIS”复选框，单击“发布”按钮，系统会在Internet信息服务中自动建立该虚拟目录，同时将应用打包并发布到IIS上，IE客户端即可浏览发布的应用，如图所示：

图22-14 自动发布

**情况二：**手动发布。方法如下：

步骤一：单击“客户端应用打包输出路径（本地）”文本框后面的“” 按钮，在弹出的文件浏览器中选择用来存储客户端应用打包文件夹的路径，选择的路径信息将会显示在“客户端应用打包输出路径（本地）”文本框里。客户端应用打包文件夹的路径选择需要注意以下2点：

（1）不要选择客户端应用所在的目录。

（2）因为操作系统的用户目录有访问权限的问题，所以不能选择用户目录、我的文档、桌面这类路径，否则客户端下载会失败。

在“虚拟目录”文本框中输入任一虚拟目录的名称（如：web），不选择“配置本机IIS”复选框，此时按下“打包”按钮，系统将应用文件打包到设置的输出路径下，如图所示：

图22-15 手动发布

步骤二：建立IIS虚拟目录

（1）点击桌面“我的电脑”图标，弹出右键菜单，选择“管理”选项，弹出“计算机管理”对话框。

（2）选择“服务和应用程序”下的“Internet信息服务”中的“网站”选项下的“默认网站”，点击“默认网站”，在弹出的右键菜单中选择“新建”→“虚拟目录”选项，如图所示：

图22-16 新建虚拟目录

（3）执行上述命令弹出虚拟目录创建向导对话框，如图所示：

图22-17 虚拟目录创建向导对话框

（4）单击“下一步”按钮，输入虚拟目录别名（如：web，也可以是其他名称），如图所示：

图22-18 输入虚拟目录名称

（5）单击“下一步”按钮，选择在步骤一中设置的“客户端应用打包输出路径（本地）”并指向打包的应用，如图所示：

图22-19 关联Web发布的文件夹目录

（6）单击“下一步”按钮，弹出对话框，对话框设置如下图所示。注意：选中“执行（如ISAPI应用程序或CGI）”选项。

图22-20 虚拟目录的访问权限设置

（7）单击“下一步”按钮，完成虚拟目录的配置，此时在IE客户端即可浏览发布的应用。

**5****、ClientView****属性设置：**

如下图所示，设置在客户端进行Web浏览时，界面上需要显示哪些项，复选框中出现‘√’表示显示，否则不显示。

图22-21 虚拟目录的访问权限设置

**自定义加载画面**

勾选该选项前的复选框该选项，该选项右侧的，被激活，单击该按钮，弹出打开对话框，添加需要的自启动画面，如下图所示：

图22-22

单击“打开”按钮，选择的图片路径自动填充到输入框中，如下图所示：

图22-23

在打包工具中设置了自定义加载画面，在Web浏览时，首先会显示用户加载的自定义画面。

**6****、第三方文件按钮：**

客户机应用在运行时，会依赖一些第三方文件和程序，所以要在各客户机安装这些第三方文件。这就需要给出所依赖文件的清单，并设置它们在客户机上的安装方式，系统根据该清单里的配置信息进行打包，生成第三方文件包。

单击该按钮弹出添加第三方文件对话框，如图所示：

图22-22 添加第三方文件对话框

运行方式包括复制、注册、运行等命令，是指第三方文件下载到客户端后执行的操作，下载过程是由系统自动完成的。

复制：是最通用的操作，是指文件下载到客户端后不进行任何操作，支持所有格式的文件。

注册：指文件下载到客户端后运行regsvr32注册该文件，支持COM组件、OCX控件。

运行：指文件下载到客户端后运行该文件，支持exe格式的文件。

注册表合并：指文件下载到客户端后，把它们合并到注册表中，支持reg格式文件。

如果用户对第三方程序文件在客户机上的安装路径有特别的要求，可以将用户提供的相应批处理文件加入到第三方文件清单中，并将批处理文件的指令设为运行。

这里，我们介绍一个比较特殊的第三方文件kxClientViewUser.ini，该文件可以用来控制IE客户端浏览应用的时间。

kxClientViewUser.ini配置文件中需要设置的参数有Demo和DemoTime。

Demo：为0表示非demo演示，为1表示demo演示。

DemoTime：当Demo=1时，该项表示demo演示的分钟数，到DemotTime后会自动关闭演示应用；当Demo=0时，浏览时间不受DemoTime的影响。示例的kxClientViewUser.ini内容为：

[License]

Demo =1;

DemoTime =1;

发布时，在图22-22中单击“添加文件”按钮加载配置好的kxClientViewUser.ini文件。发布完成后，在IE客户端可以浏览发布的应用，实时数据显示正常；1分钟后会弹出提示对话框：演示时间到！应用自动关闭，确认后退出对应用的浏览。刷新或重启IE，IE客户端又可以浏览应用，1分钟又会关闭应用。

**快速添加第三方文件**

该功能涵盖的文件范围只在KingSCADA的安装目录下，其余文件不在快速添加范围之内。即必须是随安装盘一起安装的文件。

单击图22-22
的“快速添加”按钮后，会弹出“快速添加通用控件”对话框，如下图所示：

图22-23 快速添加通用控件对话框

在这里KingSCADA提供了两个自带的第三方控件，历史趋势曲线控件和ListView控件，用户可以根据需要，通过控件前面的复选框来选择自己需要的控件进行快速添加。

例如：选择历史曲线控件模块后点击“确定”按钮，“快速添加通用控件”对话框关闭。打包工具从KingSCADA安装目录下的bin文件夹里的kxWebview.ini文件的[ThirdMod]字段中读取相应的文件添加到第三方文件列表对话框中，如下图所示：

 

图22-24 第三方文件列表对话框

第三方文件涉及到三种方式：复制、注册、运行。打包工具自动按照文件默认的属性配置。快速添加的文件和一般的第三方文件一样出现在第三方文件列表对话框中，相关配置仍可以手动修改。

***注：快速添加功能默认是打包到运行环境组件目录中的，如果修改上图中的“打包目录”，则添加需要增加依赖的公共组件。***

**添加文件夹**

单击图22-22
的“添加文件夹”按钮，会弹出“浏览文件”对话框，如下图22-25所示：

图22-25 添加文件夹对话框

选中要添加的文件夹，单击“确认”按钮，如下图22-26所示，添加的文件夹和一般的第三方文件一样出现在第三方文件列表对话框中，进行web发布时，该文件夹将复制到打包目录下的ThirdMod文件夹里，并发布到web服务器上。

图22-26 第三方文件列表对话框

**7****、数据源按钮：**

如果客户端应用涉及到与关系数据库的连接，如：MySQL、SQL2008、Oracle等，并且采用的是ODBC数据源方式连接数据库时，在Web客户端要连接数据库通常做法要实现在控制面板中手动创建ODBC数据源，为了简化该操作，KingSCADA在打包工具上提供了“数据源”按钮，可以帮助用户在Web客户端自动创建数据源。

注：

当选择好客户端应用后，数据源按钮会变为可用状态。点击该按钮后弹出“ODBC数据源管理器”对话框，如下图所示。

图22-27

对话框右侧提供了三个配置数据源的按钮。

添加：添加一个系统数据源。

配置：可配置当前选中行的数据源。

删除：可把当前选中的数据源删除。

单击“添加”按钮，弹出“系统数据源”对话框，列出了所有的系统数据源。

***注：只有在控制面板里配置为“系统DSN******”的数据源才能显示。***

图22-28

选择某个数据源，单击“OK”按钮，在“ODBC数据源管理器”对话框中新增了一个系统数据源，如下图所示，即认为该数据源会随同其它文件模块进行打包。当远程电脑使用IE访问web站点后会自动把列表里添加的数据源配置到该远程电脑上。

 

图22-29

**注意事项：**

远程数据源配置成功的前提条件是，远程电脑要正确安装数据库的ODBC驱动、或者客户端软件。否则有可能配置不成功，例如：

（1）针对MySQL数据库，KingSCADA会自动把本地的ODBC驱动打包一同发布到远程电脑。当使用IE访问站点时，会检测是否安装了MySQL驱动，如果没有安装会自行安装。所以MySQL数据库可直接配置数据源，不用事先安装驱动。

（2）对于其它类型的数据库，例如SQL Server，Oracle。由于需要客户端软件，并且驱动比较复杂，需要用户实现在客户端安装其客户端软件，数据源才能配置成功。

由于不同数据源的ODBC 驱动不同，即使同一个类型的数据源，ODBC驱动版本不同，也会影响到远程数据源的配置效果。所以使用前请尽量使ODBC驱动版本一致。

对于某些数据源，例如access，Excel数据源，在配置这些数据源时直接选中的是某个数据库文件。而不是数据库服务器地址，在远程配置时，请确保在相同路径下有相同的文件。否则配置后的数据源不能用。

请不要配置KingSCADA默认使用的数据源。例如，Alarm&Event。这个数据源是报警库默认使用的。

如果添加了数据源，在控制面板内删除了数据源。再打开远程配置数据源的对话框会自动在控制面板内新建被删除的数据源。

**8****、预览按钮：**

按下此按钮，预览发布的画面。

 

***注：如果已经发布过一次，只是更新组件或者应用，在Client******打包和发布工具界面就不必再配置IIS******，这样可以加快“发布”速度。***

---

第二十二章 web发布及IE浏览

#### 22.2.1.3  Client打包和发布工具的保存功能

单击图22-21的“发布”按钮后，会弹出“进度”对话框用来显示应用的发布进度，与此同时，图22-21的ClientPack.exe主界面中配置的内容也会被保存到ClientPack.db文件中，即实现了Client打包和发布工具的保存功能。

ClientPack.db文件中保存的配置内容包括发布画面，初始画面，发布路径，IIS虚拟目录，ClientView属性设置，第三方文件等，应用画面这一项不保存。在以后每次打包和发布客户端应用时，选定客户端应用后，上述配置信息会从ClientPack.db中载入，应用画面信息仍从应用文件中读取。

从ClientPack.db中载入信息时不做校验，在打包发布时再校验有效性，如画面是否存在，第三方文件是否存在，发布路径是否有效等，并给出相应的提示。

ClientPack.db文件采用xml格式，是在第一次客户端应用发布时自动创建的，并存放在客户端应用的目录下。而且，ClientPack.db会在每次发布时覆盖上一次的内容。

---

第二十二章 web发布及IE浏览

### 22.2.2   IE浏览发布工程

在Web客户端能够对KingSCADA中的数据进行浏览，包括实时数据、历史数据、报警数据以及事件数据。对于具有操作权限的用户还可以进行数据操作。

---

第二十二章 web发布及IE浏览

#### 22.2.2.1  浏览前的准备工作

在Web客户端浏览KingSCADA发布的画面，需要做如下准备工作：

**第一：**安装IE浏览器

安装IE6.0及以上浏览器

**第二：**浏览权限设置

***注：如果kxClientDownload.ocx******加了数字签名，则不需要配置浏览权限。***

直接在IE浏览器中选择“工具/Internet选项”菜单，打开“安全”属性页，选择“受信任的站点”图标，然后点击“站点”按钮，如图所示：

图22-30受信任的站点设置

在“将该网站添加到区域中”输入框中输入进行KingSCADAWeb发布的机器名或IP地址，取消“对该区域中的站点……验证” 选项的选择，点击“添加”按钮，再点击“确定”按钮，即可将该站点添加到信任域中。

点击“Internet选项”中的“自定义级别”按钮，弹出“安全设置”对话框，在此对话框的“重置自定义设置”中选择“安全级—低”，如图所示。然后依次确定即可。

图22-31安全级别设置

接下来就可以使用IE浏览器进行应用画面的浏览和数据操作了。

---

第二十二章 web发布及IE浏览

#### 22.2.2.2  浏览画面

使用浏览器进行画面浏览时，在地址栏中输入地址格式为：Http://WebServer机器名（或IP地址）/虚拟目录名（此处的虚拟目录名为IIS上自动或手动建立的虚拟目录的名称）。

如：http://172.16.2.124/ksweb

IIS默认站点的端口号默认为80，如果配置了80以外的端口号，需要在地址中加上端口号，如http://172.16.2.124:85/ksweb

IE浏览器中输入要浏览的画面地址后，会弹出提示对话框提示需要下载的文件个数，单击“确定”按钮开始文件的下载。

***注意：客户端下载时，如果1******个客户端同时打开多个浏览器浏览画面，则多个浏览器会同时下载同一个客户端包到同一个目录。但是由于目录文件是不能同时进行写操作的，所以在每个浏览器的下载总结中，会有文件下载失败的提示并会指出下载失败可能是由于其它程序正在使用该文件，请刷新网页重试。关掉下载总结窗口，浏览器的网页中显示：文件下载失败，无法打开KS******网页。这时，用户重新刷新一下网页，就可以正常浏览了。***

浏览界面如下图所示：

图22-32 Web客户端浏览界面

当用IE浏览器浏览画面时，鼠标滑轮滚动可以控制画面上下滚动，“鼠标左键按下+滑轮滚动”可以控制画面左右滚动。

想在Web客户端浏览到实时数据，需将KingSCADA应用运行起来。对于画面中有权限限制的图素必须进行用户登录后才可以操作。

***注：1.*** ***在Win 7******操作系统下进行web******访问时（即客户端计算机的操作系统是Win 7******），需要以administrator******登录系统，否则需要以管理员身份启动浏览器。***

***2.*** ***如果客户端应用以服务器为校时服务器，当******web******发布该客户端应用后，在******IE******端访问该客户端应用时不支持校时功能。***

***3.KingSCADA******不仅支持固定公网******IP******的方式，同时也支持使用域名解析的方式做******web******发布。***

---

第二十二章 web发布及IE浏览

#### 22.2.2.3  浏览界面的工具栏

在Web发布界面中如果选中“显示工具栏”选项的话，在IE浏览界面会出现工具栏：

命令功能描述如下：

|  |  |
| --- | --- |
| **命令** | **功能描述** |
| 打开 | 通过此按钮实现画面切换功能 |
| 关闭 | 单击此按钮关闭当前浏览的画面 |
| 登录 | 单击此按钮实现用户登录操作，在用户安全管理中建立的用户名及密码同样适用在IE浏览器上 |
| 注销 | 单击此按钮实现用户注销操作 |
| 语言 | 单击此按钮实现语言切换功能 |
| 关于 | 单击此按钮查看版本信息 |

---

第二十二章 web发布及IE浏览

#### 22.2.2.4  浏览界面的状态栏

在Web发布界面中如果选中“显示状态栏”选项的话，在IE浏览界面会出现状态栏，在状态栏中会显示当前用户名称和连接状态。

---

第二十二章 web发布及IE浏览

## 22.3         跨网关浏览

---

第二十二章 web发布及IE浏览

### 22.3.1   功能介绍

如果客户端机器和KingSCADA服务器同一个局域网，那么客户端就能获取服务器的实时、历史、报警数据并在画面上显示数据。如果客户端机器和KingSCADA服务器不在同一个局域网里，KingSCADA客户端无法访问KingSCADA服务器，也无法获取KingSCADA服务器的实时、历史、报警数据。这时，就需要借助网关代理服务在不同的网络之间建立一个管道，将网关内各机器的数据服务通过端口映射功能代理到网关外。配置网关映射需要做两步工作：

第一是要做硬件映射，即路由器需要做端口映射，建立网络的映射关系。不同局域网之间通常都部署有路由器作为网关，一般路由器提供了端口映射代理服务的功能，路由器的端口映射工作一般由公司的网管去做，需要映射的端口详见图22-39中所列端口，包括实时数据、报警库和历史库，本处不再做详细介绍。

第二就是需要做“软映射”，即通过KingSCADA的ServerAgentMapTool工具去做端口映射，KingSCADA运行时通过此处去寻找对应的网络端口。该工具不需要安装，可以直接运行。

网关映射配置完成后，不仅能在不同局域网之间建立数据通道，还能在局域网和互联网之间建立数据通道。不仅局域网外的KingSCADA客户端能监控KingSCADA服务器的数据，而且KingSCADA的Web客户端机器能在公网外监控数据。

**注：若工程为双机热备+****客户端模式，主机与从机都需要做端口映射，主机与从机的相同端口可以映射到同一路由器的不同端口，web****服务器映射IIS****端口，web****发布后即可公网浏览，主从机切换，web****端数据正常。**

---

第二十二章 web发布及IE浏览

### 22.3.2   ServerAgentMapTool主界面

在KingSCADA安装路径下的bin文件夹中找到ServerAgentMapTool可执行文件并双击，弹出ServerAgentMapTool主界面，如下图所示。

图22-35 ServerAgentMapTool主界面

**映射信息文件路径：**设置映射信息文件路径，默认为空。这里，“映射信息文件路径”文本框不允许输入，需要通过单击后面的按钮进行修改或设置。

1.        
如果“映射信息文件路径”文本框为空，单击文本框后面的按钮将弹出浏览文件夹对话框，如下图所示。

图22-36
浏览文件夹

在浏览文件夹对话框中选择web发布的客户端应用文件夹，单击“确定”按钮，路径信息将会被添加到“映射信息文件路径”文本框中，该路径下的serveragentmap.ini文件里的映射信息也将会被加载到映射信息列表里。如果该路径下没有serveragentmap.ini文件，则会自动新建一个serveragentmap.ini文件，并清空映射信息列表里的信息。

2.        
如果 “映射信息文件路径”文本框非空，则弹出提示框，提示用户是否保存现有配置信息，覆盖已有文件。

图22-37提示信息

² 
单击“是”按钮，保存当前配置信息到路径框的文件里，关闭提示画面，弹出浏览文件夹对话框。在浏览文件夹对话框中选择某个文件夹，单击“确定”按钮将选择的路径更新到路径框里，并将该路径下的serveragentmap.ini文件信息加载到映射信息列表里。如果该路径下没有serveragentmap.ini文件，则自动新建一个serveragentmap.ini文件，并清空映射信息列表里的信息。

² 
单击“否”按钮，不保存当前配置信息，关闭提示画面，弹出浏览文件夹窗口，将用户选择的路径更新到路径框里，并将该路径下的serveragentmap.ini文件信息加载到映射信息列表里。如果该路径下没有serveragentmap.ini文件，则自动新建一个serveragentmap.ini文件，并清空映射信息列表中的信息。

² 
单击“取消”按钮，关闭提示框。

**保存：**保存映射信息列表里的信息，即将“映射信息列表”里的信息保存到路径文本框路径下的serveragentmap.ini文件中，该按钮在路径框为空时不可用。当路径框非空时，单击该按钮，弹出如下所示的提示框。

图22-38
提示信息

² 单击“是”按钮，保存当前的配置信息到路径框的文件里，关闭提示画面。

² 单击“否”按钮，不保存现有配置信息，关闭提示画面。

² 单击“取消”按钮，关闭提示框。

 

**映射信息列表**

映射信息列表由两部分组成：是否启用代理、服务与代理映射信息列表。

**启用代理：**是否启用代理服务，“映射信息文件路径”文本框为空时不可用。选中（复合框中出现“√”符号）表示启用代理；不选中表示不启用。

**服务与代理映射信息列表：**列举了服务与代理的映射信息，每行代表一个映射信息，由名称、服务器IP、服务器端口、代理服务器IP和代理服务器端口组成。选中某一行信息，单击“删除”按钮，将该行信息从列表中删除。如果映射信息列表没有行被选中，“删除”按钮无效。

**映射信息配置**

名称：每个映射信息的唯一标识，不能与其他的映射信息重名。

服务器IP：服务器所在主机的局域网IP地址。

服务器端口：服务占用的端口号。

代理服务器IP: 代理服务器所在主机的外网IP地址。

代理服务器端口：对服务进行映射的端口号。

**添加：**单击该按钮，将配置的映射信息添加到服务与代理映射信息列表中。

**更新：** 更新服务与代理映射信息列表中的映射信息。操作方法：1）双击服务与代理映射信息列表中的某条信息；2）在“映射信息配置”部分修改信息的内容；3）单击“更新”按钮。

***注意：更新后的映射信息不能与其他的映射信息重名，否则更新失败。***

---

第二十二章 web发布及IE浏览

### 22.3.3   KingSCADA映射信息的配置

在完成应用的打包和发布之后，为了使外网能够访问到服务器上的数据，需要将数据服务所用的端口映射出去。步骤如下：1. 查看服务所用端口；2. 用ServerAgentMapTool配置映射信息。

若工程中包含趋势曲线控件，并且控件中添加了内网的工业库数据，则公网发布时，需将工业库的端口（默认为5678）在路由器端做映射，并且在ServerAgentMapTool工具中做映射。web发布后，即可实现公网下数据查询。

---

第二十二章 web发布及IE浏览

#### 22.3.3.1  查看服务所用端口

在服务器端的KingSCADA工程设计器中，单击菜单栏的“工具 à 端口检测”选项弹出“端口检测”对话框，如下图所示：

图22-39 端口检测

上面的对话框中列出服务端使用的所有端口信息，所用端口可分为三大类：实时数据、报警库和历史库。用户需要将所有端口用代理服务器映射出去。

---

第二十二章 web发布及IE浏览

#### 22.3.3.2  用ServerAgentMapTool配置映射信息

在KingSCADA安装路径下的bin文件夹中找到ServerAgentMapTool可执行文件并双击，弹出ServerAgentMapTool主界面，如下图所示。

图22-40 ServerAgentMapTool主界面

单击“映射信息文件路径”文本框后面的按钮，在弹出的“浏览文件夹”对话框中找到做web发布的应用文件夹路径。路径信息将会被添加到“映射信息文件路径”中，该路径下的serveragentmap.ini文件信息将被加载到映射信息列表里，如下图所示。

图22-41 加载映射信息文件

选择“启用代理”选项，在映射信息配置中输入信息名称、服务器IP和端口、代理服务器IP和端口，其中，服务器端口应为在22.4.3.1节中检测到的端口，代理服务器IP应为路由器端外网的IP地址。

鉴于用户对数据服务与代理服务的部署相当灵活、机动，自动检测和显示网络里数据服务、代理服务的功能不仅没有必要，反而会限制用户的自由应用。因此，地址与端口映射配置全部需要手动输入，并且不做有效性验证，只要求数据服务名称不重名。

图22-42 配置映射信息

单击图22-42的“添加”按钮将该映射信息添加到映射信息列表中，如此反复，直到将服务端使用的所有端口添加完毕为止。

图22-43 添加映射信息

单击“保存”按钮将添加的映射信息保存到“映射信息文件路径”下的serveragentmap.ini文件中。

上面的配置使用的是一台代理服务器，但一个系统可以有多种或多个实时、历史、报警数据服务。不同的服务会分配不同的端口号。它们可以运行在一台机子上，也可以运行在不同的主机上。同时为一个系统的这些数据服务提供代理服务的主机也可以是多个，所以，多个数据服务的代理映射可以配置给一台代理服务器，也可以配置给多台代理服务器。

---

第二十二章 web发布及IE浏览

## 22.4         Web客户端运行要求

1、支持的操作系统：Windows 7专业版和旗舰版、Windows 10专业版和旗舰版、Windows Server 2008 、

Windows Server 2012 、Windows Server 2016 （不支持家庭版）。

2、支持的浏览器：IE 6、IE 7、IE 8、IE9、IE 10、IE11（不支持Edge浏览器）。

3、不支持的功能：

 不支持虚拟键盘、

 不支持报警窗打印方法AlarmWindow.Print()的调用：

4、网络带宽要求：>10M

---

第二十二章 web发布及IE浏览

## 22.5          KxClientDownload控件属性及事件

在第三方软件中，KxClientDownload.ocx可以做为一个普通的控件进行使用，并像其他控件一样拥有一些特殊的属性和事件。下面将对KxClientDownload.ocx控件特有的属性和事件做详细的介绍。

**KxClientDownload.ocx****控件提供以下开放属性：**

1、要下载和安装的WEB网址属性。

属性名：ProjectURL。

属性类型：string。

示例：kxWebDownload1.ProjectURL = "http://localhost/bnm"

***注：客户端包要从******WEB******网站下载时必须设置的属性。***

 

2、客户端操作系统语言环境属性。

属性名：LangID。

属性类型：int。

示例：kxWebDownload1.LangID = 2052

***注：客户端包要从******WEB******网站下载时必须设置的属性。***

 

3、下载安装完毕后是否要自动启动其它ocx控件属性

属性名：AutoRunCtrl。

属性类型：bool。

示例：false---不启动，true---启动，默认为true。

***注：该属性不是必须设置的属性******。******当******AutoRunCtrl*** ***设为******true******时，如果******CtrlCLSID******所设的控件在客户端不存在，会启动失败；如果******CtrlCLSID******所设的控件已经和******KxClientDownload******控件在同一个进程里，并且不支持多实例时，也会启动失败。***

 

4、下载安装完毕后要加载的其它ocx控件的CLSID属性。

属性名：CtrlCLSID。

属性类型：string。

示例：kxWebDownload1.CtrlCLSID =
"1FCD1B26-10BF-4937-B8A6-3163FD95B579"

***注：其它******ocx******控件可以不通过******KxClientDownload******来加载启动，该属性不是必须设置的属性。***

 

5、控件名称、大小、位置属性

kxWebDownload1.Name

kxWebDownload1.Width

kxWebDownload1.Height

kxWebDownload1.Left

 

6、传递给kxClientView.ocx控件显示特性属性值属性。

kxWebDownload1.ShowDetail

kxWebDownload1.ShowProgress

kxWebDownload1.ShowStatusBar

kxWebDownload1.ShowSumInfo

kxWebDownload1.ShowToolbar

***注：只有通过******KxClientDownload******加载启动******kxClientView******控件时，才需要设置的属性。***

 

**控件事件**

1、下载安装完毕事件，将下载到客户端的路径开放出来，供kxClientView控件调用。

void OnDownloadCompleted (string FolderName，string ProjectPath)

FolderName：KxClientDownload控件从WEB服务器下载文件到客户端机器的路径字符串。例如：

“c:\My Documents\kxWebDownload\kxWebDownload\http-{{localhost{bnm”

 

ProjectPath：KxClientDownload控件从WEB服务器下载文件到客户端机器的应用名全路径，例如：

“ c:\My
Documents\kxWebDownload\kxWebDownload\http-{{localhost{bnm\

Project\bjmyl\ bjmyl.kcapp”

 

示例：

kxClientView1.AppModulePath = FolderName +
"\kxModule"

kxClientView1.ProjectPath = ProjectPath

2、SCADA中使用SendUserDefMsg(int
Param1, int Param2)函数，web发布后在发布路径下index.htm 文件中，可以找到OnUserDefScriptFunc事件，

<script type="text/javascript"
for="KxClientDownload\_ocx"event="OnUserDefScriptFunc(Param1,Param2)">

用户可以根据需要修改此文件中内容，依据参数值为条件，编写脚本，用其他的脚本语言实现。

例：在SCADA的按钮脚本中使用SendUserDefMsg(1, 2)， 在index.html文件中<script type="text/javascript"
for="KxClientDownload\_ocx"event="OnUserDefScriptFunc(Param1,Param2)">下方编写如下内容：

 if (Param1==1)

alert("warning!")  

         else

            window.close();

web发布后IE浏览，点击按钮，则会弹出warning对话框，如下图所示：

图22-44

---

第二十二章 web发布及IE浏览

## 22.6         第三方嵌入webview时的配置操作

以下介绍第三方嵌入SCADA的webview时，需要更新的文件和配置项说明，具体步骤如下：

Ø  在web发布服务器上进行web发布，例如：发布虚拟目录为：e:\web64

Ø  发布完成后，到虚拟目录web64下，编辑文件index.htm，添加一行参数，如图22-45。

<param
name="ProjectURL" value ="http://172.30.10.159/web64">

**说明：****ip****地址是****web****发布服务器的****ip****地址和发布虚拟目录**

图22-45

Ø  本版本中能够实现单画面方式有两种，以下分别介绍：

1.        
使用demo进行单画面访问

1）在SCADA安装目录下，即bin\ClientLib\libs\ClientSDK打开ClientSDK文件夹，编辑文件webDemo.html要将访问的index.htm的ip地址写入（web发布服务器）http://172.30.10.159/web64/index.htm。如图22-46所示。

图22-46

2）编辑此处关闭画面和打开画面的名称在文件webDemo.html中修改。如图22-47所示。

图22-47

3）然后在IE网页上打开webDemo.html（支持IE9及以上版本浏览）。如图22-48所示。

图22-48

2.        
使用网址方式进行单画面访问

       单画面访问功能同时支持使用网址的方式进行单画面访问。编辑完成index.htm文件后，在IE网页上输入网址来访问发布的画面:“[http:// /172.30.10.159/web64/index.htm?picName=pic1](http://172.30.10.157/testweb3/index.htm?picName=测试1)”。