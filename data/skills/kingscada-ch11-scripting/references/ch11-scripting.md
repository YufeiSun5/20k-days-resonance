第十一章 脚本

# 第十一章  脚本

---

第十一章 脚本

## 11.1 概述

脚本系统是通过一种编程语言对各种数据对象，图形对象编程，实现处理计算和操作，而达到一定控制和监视目的逻辑语言，KingSCADA提供的脚本是一种在语法上类似C语言的程序，工程人员可以利用这些程序编写逻辑控制程序，从而增强应用程序的灵活性。

KingSCADA的脚本语言支持无限字节长度。在本章我们将具体介绍脚本编辑器的使用、脚本语法、脚本的分类、脚本拷贝、导入导出以及脚本函数。

***注意：KS系统和画面的后台脚本中对工程变量进行写操作时，不对当前用户的操作权限进行校验。***

---

第十一章 脚本

## 11.2 脚本编辑器

脚本的编写是在编辑器中实现的，为了方便用户正确编辑脚本，KingSCADA脚本编辑器不仅提供一些智能提示、语法加亮、自动格式化、准确定位等功能，还提供了脚本编译功能。

脚本编辑器如图11-1所示，所有命令语言编辑器的大致界面和主要部分及功能都相同，唯一不同的是，按照触发条件的不同，在界面上“触发条件”部分会有所不同。通过下面的方法可以打开脚本编辑器（以应用程序启动脚本编辑器为例）。

在KingSCADA工程设计器的树形目录区中选择“脚本”→ “系统脚本”→ “应用程序脚本”选项，双击右侧“应用程序启动时”选项，弹出脚本编辑器，如图所示：

图11- 1 脚本编辑器

脚本编辑器包括如下几部分：

1)       
菜单栏

2)       
工具栏

3)       
关键字/保留字区域

4)       
脚本编辑区

---

第十一章 脚本

### 11.2.1    菜单栏

脚本编辑器的菜单栏包含以下菜单及菜单项：

---

第十一章 脚本

#### 11.2.1.1    文件菜单

 

|  |  |
| --- | --- |
| **命****令** | **功****能** |
|  | 单击此命令或工具栏该快捷按钮，进行脚本编译操作，如果编译通过则关闭该对话框，否则提示错误信息。 |
|  | 单击此命令或工具栏该快捷按钮，放弃对脚本编辑的操作，同时关闭该对话框。 |

---

第十一章 脚本

#### 11.2.1.2    编辑菜单

 

|  |  |
| --- | --- |
| **命****令** | **功****能** |
| 撤消 | 单击此命令或工具栏中的按钮，将取消以前执行过的命令，从最后一次操作开始 |
| 重做 | 单击此命令或工具栏中的按钮，将恢复先前撤销的命令，从最后一次操作开始 |
| 剪切 | 在脚本编辑区中选择文本后，单击此命令或工具栏中的按钮，可将其删除，并复制到剪贴板中 |
| 拷贝 | 在脚本编辑区中选择文本后，单击此命令或工具栏中的按钮，将其复制到剪贴板中，同时保留该文本 |
| 粘贴 | 单击此命令或工具栏中的按钮，将当前剪贴板中的内容粘贴到脚本编辑区中 |
| 删除 | 在脚本编辑区中选择文本后，单击此命令可将其删除 |
| 全选 | 单击此命令，选中脚本编辑区中的所有文本 |
| 查找替换 | 单击此命令弹出二级菜单，包括查找和替换两个命令，详情请参见下文 |
| 转到行 | 单击此命令可实现快速定位的功能，在弹出对话框内输入行数字后，单击“确定”按钮，系统可将光标定位到该行的行首 |
| 增加缩进 | 此命令可实现对单文本行或多文本行行首缩进的功能，相当于TAB键的功能 |
| 减少缩进 | 此命令实现的功能与增加缩进相反，减少行首缩进 |
| 注释 | 此命令可实现对选中的文本行进行注释的功能，对于注释的文本行，系统将不执行;  单行注释是在文本行的开头加注释符“//”;  多行注释是在文本行的开头加注释符“/\*”，在文本行的结尾加注释符“\*/”; |
| 取消注释 | 此命令实现的功能与注释相反，取消对文本行的注释 |

 

**查找：**单击此命令，弹出对话框如图所示：

图11- 2 查找对话框

**查找：**在此编辑框中输入要查找的文本。

**全字匹配：**选择此项，仅显示与“查找目标”字符串全字匹配的实例。

**例如：**搜索“MyObject”将返回“MyObject”，而不会返回 “CMyObject”或“MyObjectC”。

***注：全字匹配的规范是：字母和数字被看作同一类字符，其余字符均看成分隔符。***

**大小写匹配：**选择此项，仅显示与“查找目标”字符串的内容和大小写都匹配的实例。

**例如：**搜索“MyObject”，则将只返回“MyObject”，而不会返回“myobject”或“MYOBJECT”。

**向上查找：**从当前光标处向上查找。

**向下查找：**从当前光标处向下查找。

**替换：**单击此命令，弹出对话框如图所示：

图11- 3 替换对话框

**查找：**在此编辑框中输入替换的源目标。

**替换：**在此编辑框中输入替换的目的目标。

**查找按钮：**替换的源目标和目的目标设置完成后，单击“查找”按钮，系统将光标定位到源目标上，然后按下**“替换”**按钮将源目标替换为目的目标，可根据需要执行替换操作。

**替换全部按钮：**替换的源目标和目的目标设置完成后，单击“替换全部”按钮可将脚本编辑区中的所有源目标替换为目的目标。

其他设置项与查找设置项相同，在此不再赘述。

---

第十一章 脚本

#### 11.2.1.3    视图菜单

 

|  |  |
| --- | --- |
| **命****令** | **功****能** |
| 工具栏 | 显示或隐藏工具栏。工具栏分为三部分：标准、语法编辑和环境编辑。分别单击不同的选项用来显示和隐藏。 |
| 输出窗口 | 单击此命令，在脚本编辑器底部会出现输出窗口，在此窗口中显示编译过程中产生的信息 |

---

第十一章 脚本

#### 11.2.1.4    工具菜单

 

|  |  |
| --- | --- |
| **命****令** | **功****能** |
| 编译 | 单击此命令或工具栏中的按钮，可对编辑完的脚本进行编译操作，主要是语法检查 |
| 选项 | 单击此命令弹出对话框，详情请参见下文 |

 

**选项：**单击此命令弹出对话框，如图所示：

图11- 4 选择项对话框

对话框包含两个属性页：字体编辑属性页和语法颜色设置属性页。

**编辑字体属性页：**在该属性页中可设置脚本编辑器中输入文本的字体、字体的风格及字体的大小。

**语法颜色设置属性页：**在该属性页中可设置语法中不同类型文本的颜色/背景颜色。可设置的语法项包括：

²  Comment（注释）

²  Function（函数）

²  Identifier（标识符）

²  Keyword（关键字）

²  Mult-Line
comment prefix（多行注释头）

²  Mult-Line
comment prefix（多行注释尾）

²  Normal（正常文本）

²  Number（数字）

²  Operator（操作者）

²  Single
line comment prefix（单行注释头）

²  Station（站点名）

²  String（字符串）

²  Type（变量类型）

---

第十一章 脚本

#### 11.2.1.5    插入菜单

 

|  |  |
| --- | --- |
| **命****令** | **功****能** |
| 变量 | 单击此命令在编辑区中插入所需的变量 |
| 配方 | 单击此命令在编辑区中插入所需的配方 |
| 画面 | 单击此命令在编辑区中插入所需的画面 |
| 函数 | 单击此命令在编辑区中插入所需的函数 |

---

第十一章 脚本

### 11.2.2    关键字区/保留字区域

关键字/保留字选择区域，如图所示：

图11- 5 关键字/保留字区域

在此区域中列举了KingSCADA所支持的关键字、运算符、控制语句等，在脚本编辑时，可以直接在此区域中进行选择。

This是一个关键字，在画面内的事件脚本（如画面脚本、画面中图素鼠标事件脚本）中，this代表当前编辑的画面对象，在上述脚本中可通过this引用当前画面对象的属性，如this.Name、this.Background分别代表当前画面的名称和背景颜色。

---

第十一章 脚本

### 11.2.3    语法加亮功能

为了方便用户编辑脚本，KingSCADA脚本编辑器将不同的语法部分采用不同的颜色加以区别提示，如关键字、字符串、数字、注释等，KingSCADA通常会提供一个习惯的语法加亮方案，同时提供了更多的选择，用户可以根据自己的使用习惯进行自定义，定义方法参见11.2.1.4工具菜单中选择项。

---

第十一章 脚本

### 11.2.4    智能提示功能

---

第十一章 脚本

#### 11.2.4.1    Tip提示

当鼠标移动到某个变量上时，自动显示提示框提示“变量名 变量类型”，如下图所示。

图11- 6 变量的Tip提示

当鼠标移动到某个函数上时，自动显示提示框显示函数的签名，如下图所示。

图11- 7 函数的Tip提示

---

第十一章 脚本

#### 11.2.4.2    列表框提示

当用户在变量后输入“**.**”时，自动显示列表框，该框列出了该变量所有公开的属性和方法，如下图所示。

图11- 8 变量的列表框提示

***注：列表框可以通过过滤选择按钮进行选择，如仅显示属性、仅显示方法等。***

当用户输入“\\”时，自动显示列表框，该框列出了所有可用的站点名，如下图所示。选择站点后输入“**\**”，自动显示列表框，该框中列出了该站点下所有的变量、报警组等。

图11- 9 站点的列表框提示

例如，在画面脚本中输入“this.”时，自动显示列表框，该框中列出了该画面所有公开的属性，如图所示。

图11- 10 画面的列表框提示

---

第十一章 脚本

### 11.2.5   快捷键

脚本编辑器中可使用的快捷键如下表所示：

|  |  |
| --- | --- |
| **命令** | **快捷键** |
| 拷贝 | Ctrl+C |
| 剪切 | Ctrl+X |
| 粘贴 | Ctrl+V |
| 全选 | Ctrl+A |
| 撤销 | Ctrl+Z |
| 重做 | Ctrl+Y |
| 转到行 | Ctrl+G |
| 查找 | Ctrl+F |
| 替换 | Ctrl+H |
| 减少缩进 | Ctrl+L |
| 增加缩进 | Ctrl+R |
| 注释 | Ctrl+M |
| 取消注释 | Ctrl+K |

---

第十一章 脚本

## 11.3 脚本语法

该节将介绍KingSCADA支持的数据类型、运算符以及控制语句。

---

第十一章 脚本

### 11.3.1    支持的数据类型

KingSCADA支持的脚本数据类型表如下：

|  |  |  |  |
| --- | --- | --- | --- |
| **数据类型** | **描述** | **取值范围** | **精度** |
| bool | bool型 | 0 (false)，1 (true) | 精确 |
| int | 32位整数 | -2147483648~2147483647 | 精确 |
| long | 64位整数 | -9223372036854775808  ~9223372036854775807 | 负数边界值不能精确 |
| float | 32位实数，单精度浮点 | -3.4E38~3.4E38 | 3.4E-38 |
| double | 64位实数，双精度浮点 | -1.7E308～1.7E308 | 1.7E-308 |
| string | 字符串 | 长度不限 | 精确 |
| DiscTag | 引用型离散变量 | 0 (false)，1 (true) | 精确 |
| IntTag | 引用型整型变量 | -2147483648~2147483647 | 精确 |
| FloatTag | 引用型实型变量 | -9.99999968028569E37  ~9.99999968028569E37 | 小数点后6位 |
| StringTag | 引用型字符串型变量 | 最大长度128个字符 | 精确 |

---

第十一章 脚本

#### 11.3.1.1   布尔类型

布尔常量：true，false

布尔变量：布尔变量的类型符为bool，取值true和false

---

第十一章 脚本

#### 11.3.1.2    整数类型

整型常量：十进制整数，如123，-456，0。

整型变量：有符号短整型和有符号长整型：

 

|  |  |  |  |
| --- | --- | --- | --- |
| **类型名称** | **标识** | **字节数** | **范围** |
| 有符号短整型 | int | 4 | -2147483648－2147483647 |
| 有符号长整型 | long | 8 | -9223372036854775808－  9223372036854775807 |

---

第十一章 脚本

#### 11.3.1.3    实数类型

实型常量：十进制小数形式，如0.123、123.0、0.0。

实型变量：单精度实型和双精度实型：

 

|  |  |  |  |  |
| --- | --- | --- | --- | --- |
| **类型名称** | **标识** | **字节数** | **有效数字** | **范围** |
| 单精度实型 | float | 32 | 6－7 | -3.4e-37－3.4e38 |
| 双精度实型 | double | 64 | 15－16 | 10e-307－10e308 |

---

第十一章 脚本

#### 11.3.1.4    字符串类型

字符串常量：使用双引号引起来的若干字符，如“KingSCADA”，“script”等。

***注：“kingview******”和“KINGVIEW******”是不同的字符常量。***

字符串变量的类型符为string，定义形式如下：

string str1,str2;

---

第十一章 脚本

#### 11.3.1.5    引用类型

引用类型是脚本语言比较特殊的一种数据类型，与C++中的引用的概念相似，它是为支持对象的调用而增加的一种特殊类型。

KingSCADA脚本系统要求即能够在脚本中引用脚本外的对象，又要正确识别这些对象，因此必须引入对象、对象类型的概念。有关脚本对象的概念归结如下：

1、脚本语言支持对象的概念，但是不能创建和删除对象，真正的对象实例是在脚本以外的KingSCADA第三方模块中创建和销毁的。

2、KingSCADA脚本系统不能管理对象的生命周期，但是可以通过对象对外公布的属性和方法接口控制对象的行为，因此把它们称为外部可控对象。

3、在脚本系统中引用可控对象的方式有两种：

（a）直接引用方式，是在脚本语言程序中通过对象名直接引用对象，如：

externObj.Method1();

int value = externObj.Value;

（b）间接引用方式，通过在脚本语言中声明对应的对象类型的一个引用变量，如：

IntTag tagRef ;

tagRef = &externObj;

tagRef.Merhod1();

第一句声明了一个IntTag类型的引用变量，该引用变量所指向的对象类型只能是IntTag对象类型，第二句给引用变量赋值，即把外部可控对象externObj（必须是IntTag类型的示例）的地址赋值给tagRef，此时tagRef就指向externObj。第三句是通过引用变量tagRef访问其所指对象的一个方法，即间接引用了可控对象externObj。

---

第十一章 脚本

#### 11.3.1.6    引用变量

**1****、定义一个引用变量**

一般形式：

变量类型  变量名;

或者是

变量类型 变量名 = &对象名

或者是

变量类型 变量名 = &引用变量名

第一种形式是将引用变量值初始化位null，即不指向任何对象。

第二种形式是将对象的地址赋值给引用变量，变量指向一个外部可控对象。

第三中形式是将另外一个引用变量的值赋给该引用变量，即这两个引用变量指向同一个对象。

**2、** **引用变量的使用**

有关的运算符：& （取地址运算符）。

objRef = &obj;

上面语句就是将对象obj的地址赋值给引用变量objRef。

---

第十一章 脚本

#### 11.3.1.7    数组类型

数组是有序数据的集合。数组中的每一个元素都属于同一个数据类型。用一个统一的数组名和下表来唯一确定数组中的元素。

**1****、一维数组的定义方式**

类型说明符[常量表达式] 数组名;

（a）数值类型数组

int[10] a;

它表示数组名为a，此数组有10个元素，元素类型为整型。

（b）对象引用类型数组

IntTag[10] b;

它表示数组名为b，此数组有10个元素，元素类型为IntTag引用类型。

说明：

（1）数组名定名规则和变量名相同，遵循标识符定名规则;

（2）类型说明符后是用方括弧括起来的常量表达式，不能用元括弧;

（3）常量表达式表示元素的个数，即数组长度。例如在int[10]a中，10表示a数组有10个元素，下标从0开始;

（4）常量表达式中只能包含常量。也就是说，不允许对数组的大小做动态定义，即数组的大小不依赖与程序运行过程中的变量的值。

**2****、一维数组的引用**

数组必须先定义，然后使用。规定只能逐个引用数组元素而不能一次引用整个数组。

数组元素的表示形式为：

数组名[下标]

下标可以是整型常量或整型表达式。

**3****、一维数组的初始化**

对数组元素的初始化可以用一下方式实现：

**（a****）在定义数组时对数组元素赋以初值。例如：**

int[5] a = { 0, 1, 2, 3, 4 };

将数组元素的初值一次放在一对花括弧内。经过上面的定义和初始化之后

a[0] = 0,a[1] = 1, a[2] = 2, a[3] = 3; a[4] =4。

IntTag[5] b = { &tag0, &tag1, &tag2, &tag3,
&tag4 };

经过上面的定义和初始化之后，b[0] = &tag0，b[1]=&tag1，b[2]=&tag2,，b[3]=&tag3，b[4]=&tag4。

**（b****）可以只给一部分元素赋值。例如：**

int[5]a = { 0, 1, 2 };

定义a数组有5个元素，但花括弧内只提供3个初值，这表示只给前面3个元素赋初值，后2个元素为0。

IntTag[5] b = { &tag0, &tag1, &tag2 };

定义b数组有5个元素，但花括弧内只提供3个初值，表示前三个元素依次赋初值，后两个元素值为null。

---

第十一章 脚本

### 11.3.2    支持的运算符

脚本的运算符范围很宽，把除了控制语句以外的几乎所有的基本操作都作为运算符处理，例如将赋值符“＝”作为赋值运算符，方括号作为下标运算符，点作为域访问运算符等。KingSCADA脚本语言支持的运算符如下：

---

第十一章 脚本

#### 11.3.2.1    算术运算符

**1****、基本的算术运算符**

**＋：**加法运算符，或正值运算符，如3+5、+3

**－：**减法运算符，或负值运算符，如5-2、-3

**\*** **：**乘法运算符，如3\*5

**/****：**除法运算符，如5/3

**％：**模运算符，或称求余运算符，％两侧均应为整型数据，如7％3的值为1，在模数或被模数为负数的情况下，模数的符号将忽略，取模的结果的符号与被模数的符号一致，这与C语言的语法是一致的。

**2、** **自增、自减运算符**

**＋＋：**自增运算符，如i++

――：自减运算符，如i—

---

第十一章 脚本

#### 11.3.2.2    关系运算符

**>****：**大于

**<****：**小于

**= =****：**等于

**>=****：**大于等于

**<=****：**小于等于

**!=****：**不等于

---

第十一章 脚本

#### 11.3.2.3    逻辑运算符

！：非逻辑

||：或逻辑

&&：与逻辑

---

第十一章 脚本

#### 11.3.2.4    位运算符

**&****：**按位与，参与运算的两个数据，按二进制位进行“与”运算，如果两个相应的二进制位都为1，则该位的结果指为1，否则为0。

**|****：**按位或，参与运算的两个数据，按二进制位进行“或”运算，如果两个相应的二进位中只要有一个为1，该位的结果值为1。

**^****：**异或，参加运算的两个二进位同号，则结果为0，异号则为1。

**~****：**取反，是一个单元运算符，用来对一个二进制数按位取反，即将0变1，1变0。

**<<****：**左位移运算，用来将一个数的二进制位全部左移若干位。例如：a =
a<<2。

将a的二进制数左移2位，右补0。高位左移溢出，舍弃不起作用。

**>>****：**右位移运算，用来将一个数的二进制位全部右移若干位。例如：a =
a>>2。

将a的二进制数右移2位，左补0。低位右移溢出，舍弃不起作用。

---

第十一章 脚本

#### 11.3.2.5    赋值运算符

**1****、基本赋值运算符**

**＝：**赋值运算符，将一个常量、变量或表达式的值赋给一个变量。

**2****、复合赋值运算符**

在赋值赋“＝”之前加上其他运算符，可以构成复合的运算符。如果在“＝”前加一个“＋”就称了复合运算符“＋＝”。例如，可以有：

a += 3  等价于  a ＝ a ＋ 3

x \*= y + 3  等价于  x ＝ x \*（ y ＋ 3 ）

a ％=
3  等价于  a
＝ a ％ 3

a += 3  等价于  a ＝ a ＋ 3

凡是二元运算符，都可以与赋值运算符一起合成复合运算符。脚本语言规定可以使用10种复合运算符。即：

+=， －=， \*=， /=， %=， <<=， >>=， &=， ^ =， | =

---

第十一章 脚本

#### 11.3.2.6    其他运算符

除了上述运算符外还支持如下运算符：

1、指针运算符：&

2、域运算符：．

3、下标运算符：[
]

4、类型强制转换：（type）expression

强制转换类型：bool、int、long、float、double支持强制类型转换，string则不支持

用法示例：

float
fValue;

fValue
= (float)100/200;   //结果是0.5

fValue
= 100/200;     //结果是0

fValue
= (float)IntTag;

fValue
= (float)IntTag.Value;

---

第十一章 脚本

### 11.3.3    支持的脚本语句

KingSCADA支持的脚本语句如下所示：

² 
赋值语句

² 
跳转语句

² 
分支语句

² 
循环语句

---

第十一章 脚本

#### 11.3.3.1    附值语句

赋值语句用得最多，语法如下：

**变量（变量的可读写域）＝ 表达式;**

可以给一个变量赋值，也可以给可读写变量的域赋值。

**例如**：

自动开关=1;         表示将自动开关置为开(1表示开,0表示关)

颜色=2;            
将颜色置为黑色(如果数字2代表黑色)

反应罐温度.priority=3; 表示将反应罐温度的报警优先级设为3

---

第十一章 脚本

#### 11.3.3.2    跳转语句

**1****、****return**

基本形式：

return ;

或

return 表达式;

其作用为结束本次脚本执行，如果是第二种形式，则表示将表达式的值作为脚本执行完的返回值。

**2****、****break**

基本形式：

break;

其作用为从循环体内跳出循环体，即提前结束循环，接着执行循环下面的语句。

**3****、****continue**

基本形式：

continue;

其作用为结束本次循环，即跳过循环体中下面尚未执行的语句，接着进行下一次是否执行循环的判定。

continue语句和break语句的区别是：continue语句只结束本次循环，而不是终止整个循环的执行。而break语句则是结束整个循环过程，不再执行循环的条件是否成立。

**举例：**

**Tag1 = 0;**

**Tag2 = 0;**

**do**

**{**

**Tag1++;**

**continue;**

**Tag2++;  //****使用****continue****语句跳过此句**

**}while( Tag1 < 10);**

**if ((Tag1 == 10) && (Tag2 == 0) )**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

---

第十一章 脚本

#### 11.3.3.3    分支语句

**1****、****if****语句**

if语句的三种形式：

（a）if（表达式）语句

（b）if（表达式）语句1 else 语句2

（c）if（表达式1）语句1

else if（表达式2）语句2

……

else if（表达式m）语句m

else 语句n

**举例：**

**if (IntTag1 == 0)**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

**2****、****switch****语句**

switch语句是多分支选择语句。它的一般形式如下：

switch（表达式）

{

case 常量表达式1：语句1

case 常量表达式2：语句2

……

case 常量表达式n：语句n

default：语句n＋1

}

说明：

（1）switch后面括弧内的表达式，允许它为基本数据类型，不能为对象类型。

（2）当表达式的值与某一个case后面的常量表达式的值相等时，就执行此case后面的语句，若所有的case中的常量表达式的值都没有与表达式的值匹配时，就执行default后面的语句。

（3）每一个case的常量表达式的值必须互不相同，否则编译时会给出相应的警告信息，也会导致执行结果错误。

（4）各个case和default的出现的次序不影响执行结果。

（5）执行完一个case后面语句后，流程控制转移到下一个case继续执行。“case常量表达式”只是起语句标号作用，并不是在该处进行条件判断。在执行switch语句时，根据switch后面表达式的值找到匹配的入口标号，就从此标号开始执行下去，不再进行判断，因此，应该在执行一个case分支后，使流程跳出switch结构，即终止switch语句的执行，可以用break语句达到此目的。

**举例：**

**Tag1 = 11;**

**Tag2 = 0;**

**switch (IntTag1)**

**{**

**case 1:   Tag2 = 1;    break;**

**case 2:   Tag2 = 2;    break;**

**case 3:   Tag2 = 3;    break;**

**case 4+7:  Tag2 = 11;   break;**

**default:   Tag2 = 4;    break;**

**}**

**if (Tag2 == 11)**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

---

第十一章 脚本

#### 11.3.3.4    循环语句

**1****、****while****语句**

while语句用来实现“当型”循环结构。一般形式如下：

while（表达式）语句

当表达式为非0值时，执行while语句中的内嵌语句。特点是：先判断表达式，后执行语句。

注意：

（1）循环体如果包含一个以上的语句，应该用花括弧括起来，以复合语句形式出现。

（2）在循环体中应有使循环趋向于结束的语句。否则，循环可能永不结束。

**举例：**

**Tag1 = 2;**

**Tag2 = 10000;**

**while( Tag1 < Tag2)**

**{**

**Tag1++;**

**}**

**if (Tag1 == Tag2)**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

**2****、****do****－****while****语句**

do－while语句的特点是先执行循环体，然后判断循环条件是否成立。其一般形式为

do

循环体语句

while（表达式）

它是这样执行的：先执行一次指定的循环体语句，然后判别表达式，当表达式的值为“真”时，返回重新执行循环体语句，如此反复，直到表达式的值为“假”为止，此时循环结束。

**举例：**

**Tag1 = 0;**

**Tag2 = 0;**

**do**

**{**

**Tag1++;**

**Tag2++;**

**}while( Tag1 < 100);**

**if ((Tag1 == 100) && (Tag2 == 100) )**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

**3****、****for****语句**

**for****语句的一般形式为**

for（表达式1;表达式2;表达式3）语句

它的执行过程如下：

（1）先求解表达式1。

（2）求解表达式2，若其值为真，则执行for语句中指定的内嵌语句，然后执行下面第3步。如为假，则结束循环，转到第（5）步。

（3）求解表达式3。

（4）转回上面第2步骤继续执行。

（5）循环结束，执行for语句下面的一个语句

**for****语句最简单的应用形式：**

for（循环变量赋初值;循环条件;循环变量增值）语句

**举例：**

**Tag1 = 0;**

**Tag2 = 0;**

**for( Tag1 = 0; Tag1 < 100; Tag1++)**

**Tag2++;**

**if (Tag2 == 100)**

**{**

**Bool\_RunResult.Value = true;**

**}**

**else**

**{**

**Bool\_RunResult.Value = false;**

**}**

---

第十一章 脚本

## 11.4 脚本分类

KingSCADA脚本从可见性上可以分成全局事件脚本和局部事件脚本，区别在于访问对象的范围不同。全局脚本能够访问系统变量、报警、操作画面，而局部脚本除了能够访问以上这些外，还能够访问画面的变量、操作画面的图素和控件。

---

第十一章 脚本

### 11.4.1    全局事件脚本

全局事件脚本包括系统事件脚本和自定义事件脚本。

**系统事件脚本包括如下：**

² 
应用程序脚本

² 
报警事件脚本

² 
用户事件脚本

**自定义事件脚本包括如下：**

² 
数据改变脚本

² 
事件脚本

² 
热键脚本

² 
自定义函数脚本

² 
定时脚本

其中应用程序脚本、数据改变脚本、事件脚本、热键脚本和定时脚本也可以称为“后台命令语言”，它们的执行不受画面打开与否的限制，只要符合条件就可以执行。

---

第十一章 脚本

#### 11.4.1.1    应用程序脚本

应用程序脚本是指在KingSCADA运行系统应用程序启动时、运行期间和程序退出时执行的脚本程序。如果是在运行系统运行期间，该程序按照指定的时间间隔定时执行。

在KingSCADA的工程设计器树形目录下选择“脚本”→“系统脚本”→“应用程序脚本”，右侧编辑区出现应用程序脚本列表，如图所示：

图11- 11 应用程序脚本列表

双击“应用程序启动时”选项，弹出对话框，在该编辑器中输入脚本程序，该程序只在运行系统程序启动时执行一次。

双击“应用程序运行时”选项，弹出对话框，在该编辑器中输入脚本程序，并在执行周期编辑框中输入执行周期。则KingSCADA运行系统运行时，将按照该时间周期性的执行脚本程序，周期范围55-3600000毫秒。

双击“应用程序退出时”选项，弹出对话框，在该编辑器中输入脚本程序，该程序只在运行系统程序退出时执行一次。

---

第十一章 脚本

#### 11.4.1.2    报警事件脚本

报警事件脚本的介绍，请参见第12章报警与事件系统。

---

第十一章 脚本

#### 11.4.1.3    用户事件脚本

用户事件脚本的介绍，请参见第6章用户安全管理系统。

---

第十一章 脚本

#### 11.4.1.4    数据改变脚本

在KingSCADA的工程设计器树形目录下选择“脚本”→“自定义脚本”→“数据改变脚本”选项，在右侧显示区中单击“新建”按钮弹出对话框，如图所示：

图11- 12 数据改变脚本编辑器

在“变量[.域]”后面的编辑框中输入或单击按钮来选择变量名称或变量的域，这里可以连接任何类型的变量和变量的域，如离散型、整型、实型、字符串型等。当连接的变量值或变量域值发生变化时，系统会自动执行该脚本程序。

---

第十一章 脚本

#### 11.4.1.5    事件脚本

在KingSCADA的工程设计器树形目录下选择“脚本”→“自定义脚本”→“条件事件脚本”选项，在右侧显示区中单击“新建”按钮弹出对话框，如图所示：

图11- 13 事件脚本编辑器

**条件：**在该编辑框中输入脚本执行的条件，如液位==10。

**注释：**在该编辑框中输入该脚本程序的文字说明。

**进入：**在该选项页中输入当条件发生时执行的脚本程序，该程序仅执行一次。

**存在：**在该选项页中输入当条件发生期间执行的脚本程序，并在执行周期编辑框中输入执行周期。则在条件发生期间，将按照该时间周期性的执行脚本程序。

**退出：**在该选项页中输入当条件消失时执行的脚本程序，该程序仅执行一次。

---

第十一章 脚本

#### 11.4.1.6    热键脚本

在KingSCADA的工程设计器树形目录下选择“脚本”→“自定义脚本”→“热键脚本”选项，在右侧显示区中单击“新建”按钮弹出对话框，如图所示：

图11- 14 热键脚本编辑器

**热键选择区：**在“热键选择区”中定义热键，单击空按钮，弹出对话框如图所示：

图11- 15 热键选择对话框

在此对话框中选择一个键作为热键，也可以与Ctrl和Shift合成热键，用户可以根据需要进行设置。

**安全设置区：**在“安全设置区”中设置热键的安全区及操作权限，只有拥有此安全区和权限的用户才可访问热键。

**脚本编辑区：**在下面的脚本编辑区中输入运行状态下按下该热键所执行的脚本程序。

---

第十一章 脚本

#### 11.4.1.7    用户自定义函数

如果KingSCADA提供的各种函数不能满足工程的特殊需要，KingSCADA还提供了用户自定义函数功能。用户可以自己定义各种类型的函数，通过这些函数能够实现工程特殊的需要。如特殊算法、模块化的公用程序等都可通过自定义函数来实现。

自定义函数是利用类似C语言来编写的一段程序，其自身不能直接被KingSCADA触发调用，必须通过其他脚本程序来调用执行。

在KingSCADA的工程设计器树形目录选择“脚本”→“自定义脚本”→“自定义函数”选项，在右侧显示区中单击“新建”按钮弹出对话框，如图所示：

图11- 16 自定义函数脚本编辑器

在编辑器中单击下拉按钮，弹出列表项如下：

图11- 17 返回值类型列表框

**1****、自定义函数可以有六种返回值：**

void：表示函数无返回值或返回值类型为空(null)类型。

bool：表示函数返回值类型为布尔型。

int：表示函数返回值类型为短整型。

long：表示函数返回值类型为长整型。

float：表示函数返回值类型为单精度实型。

double：表示函数返回值类型为双精度实型。

string：表示函数返回值类型为字符串类型。

**2****、自定义函数构成**

（a）自定义函数类型（函数返回值类型）

（b）函数名和参数类型及名称

（c）函数体内容

**3****、自定义函数定义**

**第一步：**在函数返回值类型列表框中选择函数返回的类型

**第二步：**在返回值类型框后面的编辑框中输入该函数的名称，不能为空。函数名称的命名应该符合KingSCADA的命名规则，允许与工程变量及其他工程对象（比如报警组等）重名，但禁止使用的名称包括：KingSCADA系统自带函数，脚本关键字。函数名后应该加小括号“（）”号，如果函数带有参数，则应该在括号内声明参数的类型和参数名称，参数可以设置多个。

函数名+参数 总长度不能超过128个字符

**第三步：**在下面的脚本编辑区中输入函数体程序。在程序中可以使用自定义变量。函数体中的最后部分是返回语句。如果该函数有返回值，则使用return value（value为某个变量的名称）。对于无返回值的函数也可以使用Return，但只能单独使用Return，表示当前命令语言或函数执行结束。

**4****、自定义函数注意事项：**

自定义函数中，不支持简单类型如bool、int、long、float、double、string作为输出参数。用户可使用IntTag，FloatTag，DiscTag和StringTag作为参数类型，调用自定义函数时使用工程变量对应这些参数，如：

void
jiecheng1(long Ref, IntTag Ret)  // Ret为返回值

long
a;  //自定义变量，控制阶乘循环次数

long
mul; //自定义变量，存储阶乘运算结果

a=1;

mul=1;

if
(Ref<=0)

mul=1;

else

{

while
(a<=Ref)

{mul=mul\*a;

a=a+1;

}

}

Ret=mul;

return; 
  //函数执行结束

使用函数：jiecheng1(Tag1,Tag2);
在脚本运行后，Tag2的值会发生变化

**5、** **自定义函数举例**

例1：

函数功能：实现累加

函数返回值类型：long

函数名为：function2(long nTemp2)

函数体的内容：

nTemp2=nTemp2+1;   //累加;

return nTemp2;       //返回nTemp2的值;

定义完成后，在KingSCADA自定义函数内容区出现“long function2(long
nTemp2)”函数。调用时，如KingSCADA的整型变量为：计数2，在命令语言中输入：function2(计数2)。

例2：

函数功能：实现将给定的角度转换成弧度

函数返回值类型：float

函数名：弧度转换(float Ref)

函数体内容：

float pai;  //圆周率

float Ret;

pai=3.14159265;

Ret=(Ref/180)\*pai;

return Ret;  //返回结果

当很多的命令语言里需要一段同样的程序时，可以定义一个自定义函数，在命令语言里来调用，减少了手工的输入量，减小了程序的规模，同时也使得程序的修改和调试变得更为简明、方便。

---

第十一章 脚本

#### 11.4.1.8   定时脚本

定时脚本是指KingSCADA系统运行期间定时触发的脚本程序。

KingSCADA的工程设计器树形目录下选择“脚本”→“自定义脚本”→“定时器脚本”选项，在右侧显示区中单击“新建”按钮弹出对话框，如图所示：

图11- 18  定时脚本编辑器

**定时器名称：**在此编辑框中输入定时器的名称，用来区分不同的定时器。

**执行周期：**在此编辑框中输入脚本程序执行的间隔，以毫秒为单位，周期范围55-3600000毫秒

在脚本编辑区中输入定时触发的脚本程序，触发时间以系统启动时间为起始。

***注意：全局事件脚本中不支持调用画面内部对象以及与画面相关的函数，如showpicture()***

---

第十一章 脚本

### 11.4.2    局部事件脚本

局部事件脚本包括：

²  画面脚本

²  画面图素脚本

²  控件的事件脚本

---

第十一章 脚本

#### 11.4.2.1    画面脚本

画面脚本是指画面打开时、画面运行时和画面关闭/隐含时执行的脚本程序。

在KingSCADA画面编辑器中打开任一画面 →
单击“连接窗口”中的图标可弹出画面脚本列表，如图所示：

图11- 19 画面脚本列表

**画面打开脚本：**在图中单击“打开时”命令，弹出对话框，如下图所示，在该编辑器中输入脚本程序，该程序只在画面打开时执行一次。

图11- 20 画面打开脚本编辑器

**画面存在脚本：**在图11-19中单击“存在时”命令，弹出对话框，在该编辑器中输入脚本程序，并在执行周期编辑框中输入执行周期。则在运行环境中画面被打开时，将按照该时间周期性的执行脚本程序，周期范围55-3600000毫秒。

**画面关闭脚本：**在图11-19中单击“关闭时”命令，弹出对话框，在该编辑器中输入脚本程序，该程序只在画面关闭时或隐含时执行一次。

---

第十一章 脚本

#### 11.4.2.2    画面图素脚本

画面图素脚本通常是指鼠标触敏该图素时执行的脚本程序。如当鼠标左/右键按下或弹起时、鼠标放在该图素上、鼠标离开该图素时执行的脚本程序等。具体介绍请参见动画链接一章。

---

第十一章 脚本

#### 11.4.2.3    控件的事件脚本

控件的事件脚本是指Windows通用控件和ActiveX控件的事件所触发的脚本程序，具体介绍请参见第5章控件。

---

第十一章 脚本

## 11.5 脚本导入导出、拷功能以及自定义脚本分组功能

同一个工程中包括服务端应用组、客户端应用组、IOServer应用组，每个应用组又可以包括多个应用。

图11- 21

为了方便各应用间的脚本移植，KingScada提供了脚本导入导出的功能。

---

第十一章 脚本

### 11.5.1   脚本导入导出

为了便于理解，我们按照先脚本导出，后脚本导入的顺序介绍。

---

第十一章 脚本

#### 11.5.1.1   脚本导出

选中树形目录区“脚本”节点，鼠标右键菜单，单击“导出”菜单，弹出“应用脚本导入导出类型”对话框。

图11- 22

图11- 23

单击“确定”，弹出“另存为”对话框。在该对话框中选在要导出的路径，和导出的文件名，文件类型为KS格式。

例如：要导出定时器脚本，选择如下图所示。

图11- 24

单击“确定”，弹出“另存为”对话框。在该对话框中选在要导出的路径（C盘），和导出的文件名（Timer），文件类型为ks格式。

图11- 25

单击“保存”后，在C盘目录下生成Timer.ks的文件。

在导出脚本过程中，提供进度条显示进度，在执行结束后，在输出窗口输出执行结果信息。

图11- 26

---

第十一章 脚本

#### 11.5.1.2   脚本导入

选中树形目录区“脚本”节点，鼠标右键菜单，单击“导入”菜单，弹出“打开”对话框。

图11- 27

图11- 28

选择文件的路径下的的文件名称，文件类型ks，选择完路径和文件，单击“打开”按钮，弹出如下图所示的对话框。

图11- 29

该路径下存在的脚本类型的复选框被勾选，不存在的脚本类型的复选框不被勾选，用户勾选要导入的脚本类型，点确定，执行导入。如果脚本类型不存在，也勾选了复选框，也执行导入过程。

在导入脚本过程中，提供进度条显示进度，在执行结束后，在输出窗口输出执行结果信息。

执行导入的规则，参见11.5.2脚本拷贝规则。

---

第十一章 脚本

### 11.5.2   脚本拷贝

KingSCADA提供了脚本拷贝功能，拷贝功能具有如下应用特点：

1、脚本拷贝支持单个拷贝、多选批量拷贝。

2、脚本拷贝包括本应用拷贝、跨应用拷贝（同一工程下不同应用）。

系统脚本不提供本应用拷贝，只提供跨应用拷贝。

数据改变脚本、条件事件脚本、热键脚本、定时器脚本、自定义函数脚本提供本应用拷贝和跨应用拷贝。

3、系统脚本跨应用拷贝时，针对同名同类型脚本的拷贝，提供3种处理方式供用户选择：略过，合并，替换，“自动编号”选项不支持。

4、数据改变脚本、条件事件脚本、热键脚本、定时器脚本、自定义函数本应用拷贝时，自动按自动编号方式处理。

5、数据改变脚本、条件事件脚本、热键脚本、定时器脚本、自定义函数脚本跨应用拷贝时，针对同名同类型函数脚本的拷贝，提供4种处理方式供用户选择：略过，合并，替换，自动编号。

6、不能叠加拷贝，不能同时拷贝多种类型的脚本，即：如果点了多次同类型或不同类型脚本工具栏上的【拷贝】按钮，剪贴板只保存最后选中的脚本。

7、可以重复粘贴，即粘贴后，不自动清空剪贴板。

8、脚本拷贝时，不进行语法验证和编译查错，用户打开拷贝后生成的脚本，如果有语法错误，如找不到变量和函数，再确定时，会提示错误，无法关闭，如果不修改错误，则只能点取消，退出编辑。编译时，有语法错误的脚本不能通过编译，运行时不能被调用。

9、在作“合并”处理时，在旧有脚本的最后插入1个注释行（//\*\*\*\*\*MergeScript\*\*\*\*\*//），在注释行后插入合并进来的脚本。

10、客户端应用没有报警事件脚本，如果客户端工程导入脚本里包括了报警事件脚本，自动略过报警事件脚本，不要把报警事件脚本导入到客户端工程文件里。

---

第十一章 脚本

#### 11.5.2.1   系统脚本拷贝

系统脚本包括3种类型：应用程序脚本、报警事件脚本、用户安全验证事件脚本。

图11- 30

其中，应用程序脚本有3个脚本：应用程序启动时、应用程序运行时、应用程序关闭时。报警事件脚本有3个脚本：OnAlarmEvent\_Disc、OnAlarmEvent\_Int、OnAlarmEvent\_Float。用户安全验证事件脚本有2个脚本：OnLogOn、OnLogOff。

图11- 31

图11- 32

 

图11- 33

系统脚本的类型、个数、名称都是固定的，不能添加和删除，所以系统脚本不提供本应用拷贝，只提供跨应用拷贝，只能拷贝到其它应用的相同类型的系统脚本下，支持批量跨应用拷贝。

在工具栏上有【拷贝】和【粘贴】2个图标菜单，同时提供右键菜单。

1、当有1个或多个脚本被选中时，工具栏上的【拷贝】按钮使能，否则不使能。

2、复制某应用中某类型的1个或多个系统脚本后，该应用的脚本工具栏上的【粘贴】按钮都不使能。只有到其它应用里，并且是展开的同类型的脚本，其工具栏上的【粘贴】按钮才是使能的。

3、在其它应用的工具栏上操作【粘贴】菜单项时，会弹出“同名脚本拷贝处理”对话框，界面里的“自动编号”单选项为不可选状态，只提供“略过”，“合并”，“替换”3个选项。

略过：不进行粘贴，忽略此操作;

和并：拷贝的脚本的内容合并到被粘贴路径下的同名脚本中。

替换：用拷贝的脚本替换原来的同名脚本。

图11- 34

用户可以勾选“应用到全部”，后面的同名函数都按相同方式进行处理。如果不勾选，则每进行到1个同名同类型函数都要弹出这个界面，让用户选1次。

4、脚本拷贝时，不进行语法验证和编译查错，拷贝后生成脚本后，如果有语法错误，如找不到变量和函数等，工程运行时脚本将不能被正常调用。所以如果在上图中选择“合并”或“替换”选项时，单击上图的“确定”按钮会弹出提示框：提示用户对拷贝后的脚本进行手动编译。

图11- 35

可直接打开脚本，进行脚本编译，如果有语法错误，如找不到变量和函数等，确定时，会提示错误，无法关闭，如果不修改错误，则只能点取消，退出编辑。或通过工程编译，有语法错误的脚本也不能通过编译。

---

第十一章 脚本

#### 11.5.2.2   自定义脚本拷贝

自定义脚本包括数据改变脚本、条件事件脚本、热键脚本、定时器脚本、自定义函数，自定义脚本的个数不是固定的，可以添加和删除的，所以提供了本应用和跨应用的脚本拷贝功能，自定义脚本只能拷贝到相同类型的自定义脚本下，支持批量本应用和跨应用的脚本拷贝。

自定义脚本的工具栏如下图，有【拷贝】、【粘贴】2个图标菜单，也提供右键菜单。

1、当有1个或多个脚本被选中时，工具栏上的【拷贝】按钮使能，否则不使能。

2、复制某应用某类型的1个或多个自定义脚本后，该应用和其它应用的相同类型脚本工具栏上的【粘贴】按钮使能，其它类型脚本工具栏上的【粘贴】按钮不使能。

3、当自定义脚本在本应用拷贝时，弹出“同名脚本拷贝处理”界面，提供略过，合并，替换、自动编号4个选项。

4、自定义脚本的跨应用拷贝时，在其它应用的工具栏上操作【粘贴】按钮，如果拷贝过程中，检查到同名脚本拷贝，弹出“同名脚本拷贝处理”界面，提供略过，合并，替换、自动编号4个选项。

略过：不进行粘贴，忽略此操作;

和并：拷贝的脚本的内容合并到被粘贴路径下的同名脚本中。

替换：用拷贝的脚本替换原来的同名脚本。

自动编号：出现同名脚本时，粘贴的脚本名称后自动添加“\_编号”编号依次累加：0,1,2,3,4。。。

图11- 36

用户可以勾选“应用到全部”（默认为勾选），后面的同名函数都按相同方式进行处理。如果不勾选，则每进行到1个同名同类型函数都要弹出这个界面，让用户选1次。

5、脚本拷贝时，不进行语法验证和编译查错，拷贝后生成脚本后，如果有语法错误，如找不到变量和函数等，工程运行时脚本将不能被正常调用。所以，如果在上图中选择“合并”或“替换”选项时，单击上图的“确定”按钮会弹出提示框：提示用户对拷贝后的脚本进行手动编译。

图11- 37

可直接打开脚本，进行脚本编译，如果有语法错误，如找不到变量和函数等，确定时，会提示错误，无法关闭，如果不修改错误，则只能点取消，退出编辑。或通过工程编译，有语法错误的脚本也不能通过编译。

6、  数据改变脚本、条件事件脚本、热键脚本，除了名称不能重名，不能有非法字符外，还有一些特定的约束。

---

第十一章 脚本

### 11.5.3   自定义脚本分组功能

自定义脚本包括数据改变脚本、条件事件脚本、热键脚本、定时器脚本、自定义函数，自定义脚本的个数不是固定的，可以不同脚本进行分组，所以提供了脚本的添加分组的增删改和脚本进组移出组的功能。

当我们想建一个分组时，我们可以选中左侧的自定义脚本菜单下的各类脚本，右键进行添加分组。如图所示：

图11- 38

自定义脚本的工具栏如下图，有【移动一个分组】、【添加到当前分组】2个图标菜单。如图所示：

图11- 39

当选中一个已建好的脚本，可以选中脚本，点击移动一个分组来进行脚本的移动。如下图所示：

 

图11- 40

 

也可以选中一个分组，点击添加到当前分组，来选择脚本，将脚本移到此组来，如下图所示：

图11- 40

---

第十一章 脚本

## 11.6 脚本函数

脚本函数介绍请参见函数速查手册。